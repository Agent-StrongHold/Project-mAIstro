# Verify #1155 @ e1058db0e22758058dabb0914c3fdfa0adfa3434 (branch auto-1155) — 2026-09-08

## Verdict: NEEDS-REPAIR (regression re-introduced by develop merge)

### Decisive failure (executed by verifier, this head)
- `uv run alembic heads` -> TWO heads: `036_audit_log_org_scope` and `040`.
  Fork: `alembic/versions/036_audit_log_org_scope.py:21` down_revision='039'
  and `alembic/versions/040_capability_binding_authority.py:32` down_revision='039'.
- Fresh PG18 DB: `uv run alembic upgrade head` -> FAILED
  "Multiple head revisions are present" (db `verify1155` on auto-1155-audit-pgvector @127.0.0.1:55462).
- `uv run pytest tests/migrations -q` -> 4 failed, 13 passed, 79 skipped:
  test_audit_scope_migration.py::test_audit_scope_migration_is_the_single_head,
  test_revision_metadata.py::test_migration_chain_has_one_head,
  test_single_migration_head.py::test_there_is_exactly_one_head,
  test_single_migration_head.py::test_every_revision_is_reachable_from_the_head.

### Root cause
Repair e5ca3dbf7 fixed the fork by re-parenting 036 onto 039 and recorded the
integration note: develop f695d491 adds 040 (down_revision '039') so single-head
"must be re-reconciled at the develop merge". Merges 685c1c118 (f695d491) and
e1058db0 (5f7088dd) landed WITHOUT that reconciliation — fork restored.

### What still passes (executed this head)
- `uv run pytest packages/maistro-core/tests/persistence/test_pg_audit.py
  packages/maistro-core/tests/persistence/test_sqlite_audit.py
  packages/maistro-core/tests/security/sentinel/test_audit.py -q`
  -> 35 passed, 1 skipped (skip = test_real_postgres_two_org_scope_and_schema,
  pg_audit.py:272, requires MAISTRO_TEST_PG_DSN; cannot run at this head
  because a fresh DB cannot be migrated — see fork above).
  Includes sqlite two-org test_get_entries_org_scope_cannot_cross_tenants
  (test_sqlite_audit.py:113), persistence of org scope, system-scope default,
  composition, TypeError/ValueError rejection of unsupported filters.
- `uv run ruff check .` -> All checks passed.
- Driver check-1..4 logs: ruff/format pass, audit unit tests 80 passed 1 skipped,
  inventory ok. Driver gap repeats: tests/migrations NOT in driver logs.

### Process checks
- PR #1395 body: "Refs #1155." only — no fixes/closes/resolves. Commit messages
  5f7088dd..HEAD: no closure keywords.

### Fix direction (for repair lane)
Re-parent one of {036_audit_log_org_scope, 040} so the chain is single-headed
(e.g. 036 down_revision -> '040'), keeping tests/migrations green
(single-head + reachability + audit-scope-as-head tests), then re-run
`alembic upgrade head` on a fresh PG DB and the real-PG two-org test with
MAISTRO_TEST_PG_DSN.
