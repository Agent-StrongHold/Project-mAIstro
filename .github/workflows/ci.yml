name: CI

on:
  push:
    branches: [main, integration, develop, merge/main-into-integration]
  # No `branches:` filter. It used to be [main, integration, develop], which
  # matched on the PR's *base* — so a PR stacked on a feature branch ran none of
  # this. #151 and #138 got Registry CI and Formal Conformance only: no lint, no
  # types, no tests, no coverage, no SAST, no ratchet gates. The work was not
  # unreviewed, it was unmeasured, and every accumulated failure arrived at once
  # on retarget, against the branch least able to absorb it.
  #
  # A required-check set that silently does not apply to a whole class of PRs is
  # worse than none: the green tick means different things depending on the base
  # and nothing on the PR says which. #161.
  pull_request:
  # A merge queue evaluates a synthetic commit containing the current base plus
  # the queued candidate(s). Every required CI job must report on that SHA, not
  # merely on the feature-branch head that was green before it entered the queue.
  merge_group:
    types: [checks_requested]

# Supersede in-flight runs for the same PR or merge-group candidate instead of
# letting them pile up. Protected-branch pushes still run to completion because
# their pass/fail record is durable evidence about that exact branch SHA.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.event.merge_group.head_sha || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || github.event_name == 'merge_group' }}

permissions:
  contents: read

jobs:
  lint-and-type-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      - run: bash scripts/verify-monorepo-layout.sh
      # Before anything expensive: a merge marker on the branch means the merge
      # was never finished, and one reached `develop` in a file no other gate
      # reads (#154).
      - name: No conflict markers in tracked files
        run: python scripts/check-merge-markers.py
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: >-
          uv run mypy
          packages/maistro-core/src
          packages/maistro-server/src
          packages/maistro-turing/src
          packages/maistro-canvas/src
          packages/maistro-bootstrap/src
          packages/maistro-registry/src
          packages/maistro-evolve/src
          packages/maistro-rsi/src
          packages/maistro-design/src
      # Neither of the two above resolves an import into a package that is not
      # installed in the checking environment, and hive-conductor never is: it
      # is a flat-layout app with no wheel. So `from
      # maistro_design.systems.builtins import ...` -- a module that has never
      # existed -- passed ruff, passed mypy, and passed the tests, because a
      # bare `except Exception` turned it into a fabricated design system
      # carrying a real one's slug (#293). Static, against the source trees in
      # the workspace, so an uninstalled package is still checked and no module
      # with an import-time side effect runs here.
      - name: Cross-package imports name modules and attributes that exist
        run: python3 scripts/check-cross-package-imports.py
      # The same question one seam further out. That one asks whether an import
      # names something that exists; this asks whether the path the frontend
      # fetches reaches a route the backend registers. `fetch` does not reject
      # on a 404, so a control wired to nothing renders, responds to clicks,
      # and reports a state that never happened -- Tools Lab did exactly that
      # for four tools across three endpoints (#295). Judged against the app's
      # own route table, not a list of prefixes someone maintains by hand.
      - name: Frontend calls reach registered backend routes
        run: uv run python3 scripts/check-frontend-api-routes.py
      # The same failure mode one layer out: a document naming a component that
      # does not exist. DEPLOYMENT-STANCE.md listed a `maistro-sandbox-worker`
      # in all four supported profiles and offered Kata and gVisor as backends;
      # none of them exists, and it survived because nothing compared the prose
      # to the tree (#81).
      - name: Deployment docs name only components that exist
        run: python3 scripts/check-deployment-claims.py
      # One seam further in: a control the user can reach but not name. Every
      # password and API-key field in the Conductor took its accessible name
      # from placeholder text, which disappears once the field has a value and
      # changed with server state on two of those surfaces (#375). This refuses
      # a raw `type="password"` outside the shared components, and refuses a
      # control on a credential surface that has no way to carry a label.
      - name: Credential fields are not named by their placeholders
        run: python3 scripts/check-secret-field-labels.py
      # An accepted decision can retire a rule that is written down somewhere
      # else, and the retired phrasing then outlives it. `maistro-core`'s own
      # CLAUDE.md told agents "No `org_id` in core" long after ADR-068
      # superseded that shorthand, while the package it governs carried 214
      # `org_id` references -- and being the highest-proximity instruction
      # file, it outranked the root decision that corrected it (#386).
      #
      # Registry-driven (`quality/retired-guidance.json`), so what counts as
      # retired is declared rather than inferred. A line that names its
      # replacement passes: recording that a rule was retired is not directing
      # against it, and the fix must never be to delete the history.
      - name: Retired guidance does not still direct implementation
        run: python3 scripts/check-retired-guidance.py
      # A tracked Compose overlay shipped `API_KEYS=alice:changeme-alice`
      # alongside `REQUIRE_AUTH=true`, so it read as a ready-to-run
      # authenticated deployment whose keys are published here (#367).
      # gitleaks and detect-secrets both stayed quiet: `changeme-alice` is
      # shaped like a placeholder, which is what makes it usable and what makes
      # a secret scanner the wrong instrument. This asks the other question --
      # can this file hand someone a working credential -- and holds every
      # profile to the `${VAR:?message}` shape the rest already use.
      - name: No deployment profile hands out a credential
        run: python3 scripts/check-compose-secrets.py
      # `SENSITIVE_PATH_PATTERNS` decides which self-modifications need
      # adversarial review, and it was a hand-written list that kept falling
      # one entry behind: `local_loop.py` fast-forwards the baseline,
      # `merge.py` decides which candidates land, `code_fixer.py` runs
      # candidate code, and none of the three escalated (#303). This derives
      # the requirement from the import graph reachable out of the promotion
      # and execution entry points instead of enumerating it, so a new module
      # on that path fails the build rather than waiting to be noticed.
      - name: Every module the promotion path reaches escalates
        run: python3 scripts/check-promotion-surface.py
      # The same rule, in the two file types the Compose gate cannot read.
      # `maistro-canvas/frontend` carried
      # `postgresql+asyncpg://mcp:mcp@localhost:5441/mcp_orders` twice -- once
      # as an `os.environ.get` fallback, once as `alembic.ini`'s
      # `sqlalchemy.url` -- for a database whose port that package's Compose
      # profile publishes to the host (#432). Position, not value, is what is
      # checked: a URL in a docstring or an error message is text about a URL,
      # and only a connection *default* can be picked up by a process nobody
      # configured.
      - name: No connection default carries a literal credential
        run: python3 scripts/check-connection-credentials.py
      # Four chat handlers read `stores.chat_sessions` as a global dictionary,
      # so any authenticated user could list, read, append to or delete any
      # other user's chat by id (#312). Nothing about the text of those
      # handlers looked wrong -- they did the right thing to the wrong
      # person's rows. The scoped view fixes those four; this fixes the fifth,
      # by refusing a route module that names an owned store directly.
      - name: An owned store is only reached through its scoped view
        run: python3 scripts/check-owned-store-access.py
      # `/v1/voice/` sat in AuthMiddleware's public-prefix list for the whole
      # of this repository's history, so an entire prefix -- including a route
      # that reaches the tool loop -- answered to anyone (#316). It is one
      # string in one tuple and it reads exactly like the `/health` above it.
      # Adding a public route costs one line; reviewing it costs reading
      # everything behind it, so quality/public-routes.json is where that
      # asymmetry gets an owner, a risk, and an expiry.
      - name: Every unauthenticated path is declared and justified
        run: python3 scripts/check-public-routes.py
      # `LocalRsiLoop._run_tests` carried the comment "the test command is
      # operator-supplied config, not agent input" while POST /v1/rsi/runs
      # accepted that command over HTTP (#305). The comment was true when it
      # was written and false by the time it mattered, because nothing checks
      # a comment against its callers. quality/shell-execution.json does.
      - name: Every shell=True is declared with a caller someone vouches for
        run: python3 scripts/check-shell-execution.py

      # `Dockerfile.rsi-runner` copied the repository root into an image that
      # then runs agent-authored code, and nothing denied `.env` (#308). The
      # per-Dockerfile ignore is BuildKit-only, so two files have to agree or
      # the protection depends on which builder ran.
      - name: No secret reaches an image build context
        run: python3 scripts/check-build-context.py

  postgres:
    # A real PostgreSQL, because the alternative was proving nothing. Every
    # `test_pg_*.py` module mocks the connection and asserts on the SQL string,
    # which cannot catch a missing table, a column the store writes and the
    # schema lacks, or a naive/aware timestamp mismatch — and #122 found all
    # three once a server was actually there.
    #
    # Matrixed over the supported majors so "PostgreSQL 17-19" is a tested
    # claim rather than a README line. 19 is absent only because no pgvector
    # image ships for it yet; add it here the day one does.
    name: postgres (pg${{ matrix.postgres }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        postgres: ["17", "18"]
    services:
      postgres:
        image: pgvector/pgvector:pg${{ matrix.postgres }}
        env:
          POSTGRES_USER: maistro
          POSTGRES_PASSWORD: maistro
          POSTGRES_DB: maistro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U maistro"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DB_HOST: 127.0.0.1
      DB_PORT: "5432"
      DB_USER: maistro
      DB_PASSWORD: maistro
      DB_NAME: maistro_test
      MAISTRO_TEST_PG_DSN: postgresql://maistro:maistro@127.0.0.1:5432/maistro_test
      MAISTRO_TEST_DATABASE_URL: postgresql://maistro:maistro@127.0.0.1:5432/maistro_test
      REQUIRE_AUTH: "false"
      MAISTRO_DRY_RUN: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      # First, because it drives alembic itself and ends on an empty database:
      # applying the chain and then asking these tests to apply it again is a
      # different scenario from the one they were written for (#177).
      - name: The chain applies to an empty database, and reverses
        run: uv run pytest tests/migrations/test_migration_chain.py -v --tb=short
      # The migration chain is itself under test here: it could not run to
      # completion against a clean database before this change.
      - name: Apply migrations
        run: uv run alembic upgrade head
      - name: Migrations are reversible
        run: |
          uv run alembic downgrade base
          uv run alembic upgrade head
      - name: Persistence conformance
        run: uv run pytest packages/maistro-core/tests/persistence -v --tb=short
      - name: Container wires the PostgreSQL backend
        run: uv run pytest packages/maistro-core/tests/test_container_postgres.py -v --tb=short
      # The canonical Workspace (#516). `MAISTRO_REQUIRE_PG_LEGS` here rather
      # than in the job env: the suites above predate it and a leg that has
      # never run must not start failing the job on this change. For this one
      # the point is precisely that the PostgreSQL leg cannot skip -- an
      # untested durable store is what the whole suite exists to prevent, and
      # a skip and a pass are the same green tick.
      - name: Workspace stores conform across memory, SQLite and PostgreSQL
        env:
          MAISTRO_REQUIRE_PG_LEGS: "1"
        run: uv run pytest packages/maistro-core/tests/workspaces -v --tb=short

  object-storage:
    # MinIO, not moto. The default `test` job already runs the archive suite
    # against an in-process moto server, which is a real HTTP S3 implementation
    # and enough to catch our own mistakes. MinIO is what people actually run,
    # so it is what catches the difference between "S3-compatible" and
    # "compatible with the parts moto implements" (ADR-082226-f436 decision 3).
    name: object storage (MinIO)
    runs-on: ubuntu-latest
    env:
      MAISTRO_TEST_S3_ENDPOINT: http://127.0.0.1:9000
      AWS_ACCESS_KEY_ID: maistro
      AWS_SECRET_ACCESS_KEY: maistro-secret
      AWS_DEFAULT_REGION: us-east-1
      REQUIRE_AUTH: "false"
      MAISTRO_DRY_RUN: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev --extra s3
      # `docker run`, not a `services:` container. MinIO's own image needs an
      # explicit `server /data` command and service containers cannot set one,
      # which is why this used `bitnami/minio` — a repackaging that starts the
      # server by default. Bitnami withdrew that tag and the job began failing
      # on `manifest unknown`, so the dependency on someone else's repackaging
      # is gone: the upstream image, pinned, started here.
      - name: Start MinIO
        run: |
          docker run -d --name minio \
            -p 9000:9000 \
            -e MINIO_ROOT_USER=maistro \
            -e MINIO_ROOT_PASSWORD=maistro-secret \
            minio/minio:RELEASE.2025-04-22T22-12-26Z server /data
          for _ in $(seq 1 40); do
            if curl -fsS http://127.0.0.1:9000/minio/health/live >/dev/null; then
              echo "minio ready"
              exit 0
            fi
            sleep 2
          done
          echo "minio did not become ready" >&2
          docker logs minio >&2 || true
          exit 1
      - name: Archive conformance against MinIO
        run: uv run pytest packages/maistro-core/tests/archive -v --tb=short

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: |
          set -e
          # Bounded: an unbounded apt-get or curl here hangs the whole job with
          # no output. Observed 2026-08-19 — quality.yml sat 29 minutes on this
          # step (past its own timeout-minutes) with every later step pending,
          # because the fetch from dl.filippo.io never returned and nothing
          # bounded it. `|| true` tolerates a *failed* apt, not a wedged one.
          timeout 180 sudo apt-get update || true
          timeout 180 sudo apt-get install -y age || true
          # bubblewrap is the Tier-3 sandbox backend (#76, ADR-093). Installed
          # rather than skipped-around: its integration tests are the only place
          # the isolation claim is checked against the kernel instead of against
          # our own argv, and a suite where they always skip proves nothing.
          timeout 180 sudo apt-get install -y bubblewrap || true
          # Ubuntu 24.04 ships kernel.apparmor_restrict_unprivileged_userns=1,
          # which leaves `bwrap` installed but unable to build a namespace --
          # it fails at "loopback: Failed RTM_NEWADDR". Detection treats that
          # as "no Tier 3" and the integration tests skip, correctly but
          # uselessly. Relaxing it here makes the runner resemble a host that
          # can actually run the sandbox, so the kernel assertions execute.
          sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 || true
          if ! command -v age-keygen &> /dev/null; then
            echo "Installing age-keygen from source..."
            mkdir -p /tmp/age && cd /tmp/age
            curl -fsSL --max-time 120 --retry 3 --retry-delay 5 --retry-all-errors \
              https://dl.filippo.io/age/latest/age-linux-amd64.tar.gz | tar xz
            ls -la /tmp/age/
            sudo mv /tmp/age/age /tmp/age/age-keygen /usr/local/bin/ || exit 1
            echo "Verifying installation..."
            age-keygen --help > /dev/null && echo "✓ age-keygen ready" || exit 1
          fi
      - run: uv sync --locked --extra dev --extra bootstrap
      - run: uv run pytest packages/maistro-bootstrap/tests -v --tb=short
      - run: uv run pytest packages/maistro-core/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-server/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-canvas/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-turing/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-turing/backend/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-design/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest tests/ --ignore=tests/tools/registry -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - run: uv run pytest packages/maistro-rsi/tests packages/maistro-evolve/tests -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
          PYTHONPATH: packages/maistro-rsi/src:packages/maistro-evolve/src
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            packages/hive-conductor/frontend/package-lock.json
            packages/maistro-canvas/frontend/package-lock.json
      - run: npm ci && npm run lint && npm run build
        working-directory: packages/hive-conductor/frontend
      - run: npm ci && npm run test:ci
        working-directory: packages/maistro-canvas/frontend
      - run: npm run lint
        working-directory: packages/maistro-canvas/frontend
      - run: npm run build
        working-directory: packages/maistro-canvas/frontend
      - run: npm audit --audit-level=high
        working-directory: packages/maistro-canvas/frontend
      - run: uv pip install --system -r packages/hive-conductor/backend/requirements.txt
      - run: python -m pytest packages/hive-conductor/backend/tests -v --tb=short
      # Every step above runs one tree in its own process, which is what let a
      # leaked HTTP transport couple two of them for as long as it did: the
      # root suite left a MockTransport answering 200 to every request, two
      # Conductor tests read an unreachable URL as reachable, and no job ever
      # put those trees in the same process to notice (#414).
      #
      # So one step does. Not a substitute for the per-tree steps -- those give
      # the readable failure and the coverage split -- but the only place a
      # suite's dependence on which sibling shared its interpreter can show up
      # at all. A suite that passes because of how the jobs happen to be
      # partitioned is one refactor from a red that reads as a flake.
      - name: The trees pass in one process too (no cross-suite leakage)
        run: uv run pytest tests/ packages/hive-conductor/backend/tests packages/maistro-design/tests -q --timeout=60
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      - name: Suite inventory matches collected node IDs (C1/#286)
        run: python scripts/check-suite-inventory.py

  durable-events:
    # The PostgreSQL legs of the durable-event suites (#135) skip without a
    # server, and a skipped leg is untested rather than passing — which is how
    # "durable events" stayed in-memory on PostgreSQL unnoticed in the first
    # place. This job is what makes those legs actually run.
    #
    # Plain postgres:17, not pgvector/pgvector:pg17: nothing here needs the
    # `vector` extension, because the schema-agreement test renders revision
    # 004 on its own rather than walking the chain from 001.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: maistro_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      MAISTRO_TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/maistro_test
      # Turns "no server, skip" into a failure, in this job only. Without it a
      # misconfigured service container would leave this job green with every
      # PostgreSQL leg skipped — reporting the absence of the gap rather than
      # its closure.
      MAISTRO_REQUIRE_PG_LEGS: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      - name: Durable event stores conform across memory, SQLite and PostgreSQL
        run: uv run pytest packages/maistro-core/tests/events -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"
      # Named file, not the directory: `test_migration_chain.py` walks the chain
      # from 001, whose first statement is `CREATE EXTENSION vector` — which the
      # plain postgres:17 above cannot serve. It runs in `postgres` instead,
      # which is on the pgvector image.
      - name: Migration 004 and ensure_event_schema describe the same tables
        run: uv run pytest tests/migrations/test_event_schema_agreement.py -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"

  strike-ladder:
    # The PostgreSQL leg of the strike conformance suite (#134) skips without a
    # server, and the plain `test` job has none — so the suite that exists to
    # prove `PgStrikeTracker` matches the protocol was only ever exercising
    # `InMemoryStrikeTracker`. A skipped leg is untested, not passing, and that
    # is precisely how the original defect survived: `PgStrikeTracker` returned
    # `dict` from both methods and nothing ran it against `Gate`'s expectations.
    #
    # A separate job from `durable-events` above, not a second step in it: they
    # want different databases, and a shared server would let one suite's
    # leftover schema decide whether the other's passes.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: strike_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      MAISTRO_TEST_DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/strike_test
      # Turns "no server, skip" into a failure, in this job only. Without it a
      # misconfigured service container leaves the job green with the durable
      # leg skipped — reporting the absence of the gap rather than its closure.
      MAISTRO_REQUIRE_PG_LEGS: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      - name: Strike ladder conforms on both in-memory and PostgreSQL
        run: uv run pytest packages/maistro-core/tests/security/test_strike_tracker_conformance.py -v --tb=short
        env:
          REQUIRE_AUTH: "false"
          MAISTRO_DRY_RUN: "1"

  hive-conductor-e2e-ui:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run PM workflow Playwright UI e2e against a live hive-conductor
        timeout-minutes: 14
        working-directory: packages/hive-conductor
        run: |
          docker compose -f docker-compose.test.yml --profile test up --build \
            --abort-on-container-exit --exit-code-from e2e-tests e2e-tests
      - name: Diagnose — container state and per-service logs
        if: always()
        working-directory: packages/hive-conductor
        run: |
          echo "::group::compose ps (did hive ever start and report healthy?)"
          docker compose -f docker-compose.test.yml ps -a || true
          echo "::endgroup::"
          echo "::group::hive service logs"
          docker compose -f docker-compose.test.yml logs --no-color --tail=300 hive || true
          echo "::endgroup::"
          echo "::group::e2e-tests (playwright) logs"
          docker compose -f docker-compose.test.yml logs --no-color --tail=200 e2e-tests || true
          echo "::endgroup::"
          echo "::group::is the app reachable, and what does GET / return?"
          docker compose -f docker-compose.test.yml run --rm --no-deps -T \
            --entrypoint sh e2e-tests -c \
            'wget -S -T 10 -O /dev/null http://hive:8101/ 2>&1 | head -40; echo "exit=$?"' || true
          echo "::endgroup::"
      - name: Tear down
        if: always()
        working-directory: packages/hive-conductor
        run: docker compose -f docker-compose.test.yml down -v

  hive-conductor-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Run PM workflow e2e against a live hive-conductor
        working-directory: packages/hive-conductor
        run: |
          docker compose -f docker-compose.test.yml --profile test up --build \
            --abort-on-container-exit --exit-code-from api-tests api-tests
      - name: Dump hive logs
        if: always()
        working-directory: packages/hive-conductor
        run: |
          docker compose -f docker-compose.test.yml ps -a || true
          docker compose -f docker-compose.test.yml logs --no-color --tail=200 || true
      - name: Tear down
        if: always()
        working-directory: packages/hive-conductor
        run: docker compose -f docker-compose.test.yml down -v

  wheel-imports:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - name: Build every package wheel
        run: |
          set -euo pipefail
          rm -rf dist
          for p in packages/*/; do
            [ -f "$p/pyproject.toml" ] || continue
            echo "::group::build $p"
            uv build "$p" --out-dir dist/
            echo "::endgroup::"
          done
          ls -1 dist/*.whl
      - name: Import wheels from a clean venv
        run: python scripts/verify-wheel-imports.py --dist dist/ --python 3.12

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      - run: uv pip install pip-audit
      - run: uv pip freeze --exclude-editable > /tmp/deps.txt
      - run: uv run pip-audit --strict --format=json -r /tmp/deps.txt > /tmp/pip-audit.json || true
      - run: python scripts/pip_audit_gate.py /tmp/pip-audit.json

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # buildx + the GitHub Actions cache backend, so an unchanged Dockerfile
      # reuses layers across runs instead of re-pulling and re-installing from
      # nothing every time. `docker-build` is the longest job in the suite --
      # the wall-clock floor under how fast any candidate can go green -- and
      # every build below ran with zero cache before this. Scopes are per
      # image (`engine`, `hive-conductor`, `engine-research`, `rsi-runner`) so
      # one Dockerfile's cache cannot evict another's, and security.yml's
      # `containers` job uses the SAME scope names so a main-targeted PR can
      # warm-start from whatever a develop-targeted run already built.
      - uses: docker/setup-buildx-action@v3

      # Fetch the base images first, retrying (#204). This job used to fail on a
      # registry reset before touching a line of repository content — a red
      # required check that nobody could re-run, because the GitHub App gets
      # `403 Resource not accessible by integration` on `rerun-failed-jobs`.
      #
      # Only the fetch is retried here. A genuine build failure still fails on
      # its first attempt below, so this buys reliability without hiding
      # breakage. The image list is parsed from the Dockerfiles rather than
      # written here, so it cannot drift from what is actually built.
      #
      # This protects only classic/daemon-BuildKit builds. The buildx-driven
      # builds below run in setup-buildx-action's own `docker-container`
      # driver -- an isolated builder with its own image store the host
      # daemon's retried pull never reaches -- so they carry their own retry
      # via buildx-build-retry.sh instead (Codex, #683).
      - name: Pre-pull base images (retry transient registry faults)
        run: scripts/prepull-base-images.sh Dockerfile packages/hive-conductor/Dockerfile

      - name: Build maistro-engine (cached)
        run: >-
          scripts/buildx-build-retry.sh --
          -f Dockerfile -t maistro-engine:test
          --load --cache-from type=gha,scope=engine
          --cache-to type=gha,scope=engine,mode=max .
      - name: Boot the shipped engine image on an empty PostgreSQL 18 volume
        shell: bash
        run: |
          set -euo pipefail
          network=maistro-engine-smoke
          volume=maistro-engine-smoke-pgdata
          postgres=maistro-engine-smoke-postgres
          engine=maistro-engine-smoke-api

          cleanup() {
            status=$?
            if [[ $status -ne 0 ]]; then
              docker logs "$engine" 2>&1 || true
              docker logs "$postgres" 2>&1 || true
              docker ps -a || true
            fi
            docker rm -f "$engine" "$postgres" >/dev/null 2>&1 || true
            docker network rm "$network" >/dev/null 2>&1 || true
            docker volume rm "$volume" >/dev/null 2>&1 || true
            exit "$status"
          }
          trap cleanup EXIT

          docker network create "$network"
          docker volume create "$volume"
          docker run -d --name "$postgres" --network "$network" \
            -e POSTGRES_USER=maistro \
            -e POSTGRES_PASSWORD=maistro-smoke-password \
            -e POSTGRES_DB=maistro \
            -e PGDATA=/var/lib/postgresql/18/docker \
            -v "$volume:/var/lib/postgresql" \
            pgvector/pgvector:pg18
          for _ in $(seq 1 40); do
            if docker exec "$postgres" pg_isready -U maistro >/dev/null 2>&1; then
              break
            fi
            sleep 2
          done
          docker exec "$postgres" pg_isready -U maistro

          docker run -d --name "$engine" --network "$network" -p 8000:8000 \
            -e DB_HOST="$postgres" \
            -e DB_PORT=5432 \
            -e DB_NAME=maistro \
            -e DB_USER=maistro \
            -e DB_PASSWORD=maistro-smoke-password \
            -e ROUTER_API_KEY=0123456789abcdef0123456789abcdef \
            -e 'API_KEYS=["smoke-access-token"]' \
            -e REQUIRE_AUTH=true \
            -e LITELLM_BASE_URL=http://unused-gateway:4000 \
            -e LITELLM_MASTER_KEY=smoke-gateway-key \
            maistro-engine:test

          for _ in $(seq 1 60); do
            if curl -fsS http://127.0.0.1:8000/health/live >/dev/null; then
              break
            fi
            sleep 2
          done
          curl -fsS http://127.0.0.1:8000/health/live
          curl -fsS http://127.0.0.1:8000/health/ready
          docker exec "$postgres" psql -U maistro -d maistro -Atc \
            "SELECT version_num FROM alembic_version" | grep -Eq '^[0-9a-f]+$'

          # Recreate PostgreSQL against the same 18-style parent volume and
          # prove the migrated cluster, not an anonymous volume, comes back.
          docker rm -f "$engine" "$postgres"
          docker run -d --name "$postgres" --network "$network" \
            -e POSTGRES_USER=maistro \
            -e POSTGRES_PASSWORD=maistro-smoke-password \
            -e POSTGRES_DB=maistro \
            -e PGDATA=/var/lib/postgresql/18/docker \
            -v "$volume:/var/lib/postgresql" \
            pgvector/pgvector:pg18
          for _ in $(seq 1 40); do
            if docker exec "$postgres" pg_isready -U maistro >/dev/null 2>&1; then
              break
            fi
            sleep 2
          done
          docker exec "$postgres" psql -U maistro -d maistro -Atc \
            "SELECT version_num FROM alembic_version" | grep -Eq '^[0-9a-f]+$'
      - name: Build hive-conductor (cached)
        run: >-
          scripts/buildx-build-retry.sh --
          -f packages/hive-conductor/Dockerfile -t hive-conductor:ci
          --load --cache-from type=gha,scope=hive-conductor
          --cache-to type=gha,scope=hive-conductor,mode=max .
      # The static gate above reads the ignore files. This one builds the
      # actual RSI runner from a context carrying canary secrets and proves
      # none is in any layer -- because the question is what Docker did, not
      # what the rules say it should have (#308).
      #
      # The canaries are planted at the root AND under `packages/`, and one
      # inside the re-included frontend bundle. Root-only planting is how a
      # whole class of root-relative patterns passed this job while denying
      # nothing below the context root (Codex, #510).
      #
      # `docker build` twice, once per builder: BuildKit honours
      # Dockerfile.rsi-runner.dockerignore and the classic builder honours the
      # root one, and the whole point is that neither route leaks.
      - name: Canary secrets cannot reach the RSI runner image (BuildKit and classic)
        run: |
          set -euo pipefail
          CANARY="rsi-canary-$(date +%s)-do-not-ship"
          printf 'DB_PASSWORD=%s\nOPENAI_API_KEY=%s\n' "$CANARY" "$CANARY" > .env
          printf '%s\n' "$CANARY" > rsi-canary.pem
          mkdir -p rsi-reports && printf '%s\n' "$CANARY" > rsi-reports/previous-run.json

          # The same secrets again, BELOW the context root. Docker matches
          # relative to that root, so `*.p12` and `id_rsa*` denied the root
          # copies and nothing else -- and this build copies the whole
          # `packages` tree. A canary planted only at the root passed while an
          # operator's `packages/*/id_rsa` rode into the image (Codex, #510).
          #
          # An existing package, not a new directory: `members = ["packages/*"]`
          # makes every child of `packages/` a workspace member, and one
          # without a pyproject.toml fails `uv sync` inside the build.
          deep=packages/maistro-core
          printf 'DB_PASSWORD=%s\n' "$CANARY" > "$deep/.env"
          printf '%s\n' "$CANARY" > "$deep/.env.local"
          printf '%s\n' "$CANARY" > "$deep/gateway.env"
          printf '%s\n' "$CANARY" > "$deep/rsi-canary.pem"
          printf '%s\n' "$CANARY" > "$deep/client.p12"
          printf '%s\n' "$CANARY" > "$deep/client.pfx"
          printf '%s\n' "$CANARY" > "$deep/id_rsa"
          printf '%s\n' "$CANARY" > "$deep/id_ed25519"
          printf '%s\n' "$CANARY" > "$deep/backup.age"
          printf '%s\n' "$CANARY" > "$deep/.age-key.txt"
          printf '%s\n' "$CANARY" > "$deep/recovery-phrase.txt"
          mkdir -p "$deep/secrets" && printf '%s\n' "$CANARY" > "$deep/secrets/token"

          # And one inside the generated bundle the ignore files deliberately
          # re-include for the backend image. That exception is the only rule
          # here that UNDOES a denial; a secret beneath it is what makes rule
          # ORDER load-bearing rather than a detail.
          bundle=packages/hive-conductor/frontend/dist
          mkdir -p "$bundle" && printf 'API_KEY=%s\n' "$CANARY" > "$bundle/.env"

          # A function rather than a long single-quoted trap: the paths are
          # already in variables, and a cleanup nobody can read is a cleanup
          # that leaves a canary in the checkout.
          cleanup() {
            rm -f .env rsi-canary.pem
            rm -rf rsi-reports
            rm -f "$deep"/.env "$deep"/.env.local "$deep"/gateway.env \
                  "$deep"/rsi-canary.pem "$deep"/client.p12 "$deep"/client.pfx \
                  "$deep"/id_rsa "$deep"/id_ed25519 "$deep"/backup.age \
                  "$deep"/.age-key.txt "$deep"/recovery-phrase.txt "$bundle"/.env
            rm -rf "$deep"/secrets
          }
          trap cleanup EXIT

          # `-xdev` keeps the walk on the image's own filesystem, which is where
          # every COPY lands. Without it the search descends into /proc and
          # matches `/proc/self/cmdline` -- its own command line -- so the check
          # reported a leak that was itself, and spent twenty minutes doing it.
          #
          # The needle crosses as an environment variable rather than inside the
          # command for the same reason one level down: /proc/self/environ would
          # carry it too, and the next person to drop `-xdev` should not get a
          # second way to match nothing but themselves.
          # Double-quoted with the expansion escaped, so `$NEEDLE` reaches the
          # variable as text and the *container's* shell expands it. The
          # single-quoted spelling meant the same thing and drew shellcheck
          # SC2016 ("expressions don't expand in single quotes") -- which is
          # exactly what was intended, but a lint you have to explain on every
          # read is worse than a spelling that needs no explanation.
          scan="find / -xdev -type f -exec grep -l \"\$NEEDLE\" {} + 2>/dev/null | head -20"

          for builder in 1 0; do
            echo "::group::DOCKER_BUILDKIT=$builder"
            image="maistro-rsi-runner:canary-$builder"
            DOCKER_BUILDKIT=$builder docker build -f Dockerfile.rsi-runner -t "$image" .

            # Positive control, first: the same search, in the same image, over
            # a canary that IS there. An absence check that cannot find a
            # present secret passes for the wrong reason -- which is exactly the
            # failure mode this job exists to rule out one level up.
            control=$(docker run --rm -e NEEDLE="$CANARY" --entrypoint sh "$image" \
              -c "printf '%s' \"\$NEEDLE\" > /image-scan-control; $scan")
            # Containment rather than equality: if the image also leaks, the
            # control run returns two paths, and that is the absence check's
            # finding to report -- not a false "the scan is broken".
            case "$control" in
              *"/image-scan-control"*) ;;
              *)
                echo "::error::the filesystem scan cannot find a secret it was handed (DOCKER_BUILDKIT=$builder)"
                exit 1
                ;;
            esac

            # The filesystem the loop actually sees.
            found=$(docker run --rm -e NEEDLE="$CANARY" --entrypoint sh "$image" -c "$scan")
            if [ -n "$found" ]; then
              echo "::error::the canary is in the image filesystem (DOCKER_BUILDKIT=$builder): $found"
              exit 1
            fi

            # And the layers, because a file deleted in a later layer is still
            # in the earlier one and still `docker save`-able.
            docker save "$image" -o /tmp/canary.tar
            if tar -xOf /tmp/canary.tar | grep -qa "$CANARY"; then
              echo "::error::the canary is in an exported layer (DOCKER_BUILDKIT=$builder)"
              exit 1
            fi
            rm -f /tmp/canary.tar

            # And the build history, where a bad ARG or ENV would show.
            if docker history --no-trunc "$image" | grep -qa "$CANARY"; then
              echo "::error::the canary is in the image history (DOCKER_BUILDKIT=$builder)"
              exit 1
            fi
            echo "::endgroup::"
          done
          echo "ok: the canary reached neither filesystem, layers nor history, under either builder"

      # The other three real images, built here rather than only in
      # security.yml's `containers` job -- which runs on main-targeted PRs,
      # merge groups into main, and the nightly schedule, and therefore never
      # on a develop PR. That gap is how `Dockerfile.research` reached a state
      # where it could not be built at all and nothing said so for as long as
      # it took someone to try (#346).
      #
      # Build only: scanning stays in security.yml, because a Trivy database
      # download on every PR is a cost this job should not carry. What this
      # buys is the cheap half -- "does it still build" -- on the branch where
      # the change is actually made.
      # `maistro-engine:ci` is `-f Dockerfile .` on the same context this job
      # already built as `maistro-engine:test` above -- byte-for-byte the same
      # build under a second tag. A tag alias proves exactly as much as a
      # second `docker build` would and costs nothing.
      - run: docker tag maistro-engine:test maistro-engine:ci
      - name: Build maistro-engine-research (cached)
        run: >-
          scripts/buildx-build-retry.sh --
          -f Dockerfile.research -t maistro-engine-research:ci
          --load --cache-from type=gha,scope=engine-research
          --cache-to type=gha,scope=engine-research,mode=max .
      - name: Build maistro-rsi-runner (cached)
        run: >-
          scripts/buildx-build-retry.sh --
          -f Dockerfile.rsi-runner -t maistro-rsi-runner:ci
          --load --cache-from type=gha,scope=rsi-runner
          --cache-to type=gha,scope=rsi-runner,mode=max .

  workflow-lint:
    runs-on: ubuntu-latest
    env:
      ACTIONLINT_VERSION: "1.7.7"
      SHELLCHECK_VERSION: "0.10.0"
    steps:
      - uses: actions/checkout@v4
      # `--retry` on both: these fetch from github.com/releases, which returns a
      # 502 often enough to matter. One did on this job, and the whole check
      # went red having linted nothing — a red that says nothing about the
      # branch is worse than a slow check, because someone has to spend a cycle
      # establishing that it means nothing.
      #
      # `--retry-all-errors` is the load-bearing flag: without it curl retries
      # only what it considers transient, and a 502 with `-f` is not on that
      # list. `-f` still fails the step after the last attempt, and `set -o
      # pipefail` still fails it if tar rejects what arrived.
      - name: Install actionlint + shellcheck
        run: |
          set -euo pipefail
          curl -fsSL --retry 3 --retry-delay 2 --retry-all-errors "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz" \
            | tar xz -C /tmp actionlint
          curl -fsSL --retry 3 --retry-delay 2 --retry-all-errors "https://github.com/koalaman/shellcheck/releases/download/v${SHELLCHECK_VERSION}/shellcheck-v${SHELLCHECK_VERSION}.linux.x86_64.tar.xz" \
            | tar xJ -C /tmp
          install -m0755 "/tmp/shellcheck-v${SHELLCHECK_VERSION}/shellcheck" /tmp/shellcheck-bin
      - name: "actionlint (with shellcheck over run: blocks)"
        run: /tmp/actionlint -no-color -oneline -shellcheck /tmp/shellcheck-bin
      # actionlint above shellchecks the `run:` blocks inside workflows. The
      # standalone scripts under tools/ are not workflows and were linted by
      # nobody, which is how `run_rsi_isolated.sh` came to interpolate a
      # network-derived roster into a `bash -lc` payload (#309). All three are
      # clean at this severity today, so the gate starts with no backlog.
      #
      # scripts/*.sh is deliberately not in scope: one of them carries a
      # pre-existing SC2155 that is nothing to do with this, and a gate that
      # arrives red teaches people to ignore it. Widening the glob is a
      # one-line change once that warning is fixed.
      - name: shellcheck the standalone scripts under tools/
        run: /tmp/shellcheck-bin --severity=warning tools/*.sh
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install PyYAML
        run: pip install pyyaml
      # The check-name contract #162 pins branch protection against. It lives
      # in this job because it is a check about workflow shape, next to the
      # other one — and because a rename is only caught if the gate runs on the
      # PR that does the renaming, which after #161 is every PR.
      - name: PR check contract matches the workflows
        run: python3 scripts/check-required-checks.py
      # Every workflow installs uv through .github/actions/setup-uv, pinned to
      # one exact version (#213, ADR-082526-3011). This job is the right home
      # for the same reason as the check above: it is a claim about workflow
      # shape, and a direct `astral-sh/setup-uv` reintroduced by a later PR is
      # only caught if the gate runs on that PR.
      - name: uv is installed through the one pinned wrapper
        run: python3 scripts/check-uv-setup.py
      # A workflow that pushes with the default GITHUB_TOKEN produces heads
      # ci.yml and quality.yml can never run on, so the PR reads as checked
      # while the gate set has not seen the code (#262). Static, like the two
      # checks above it -- no token, works on a fork.
      - name: workflows make no writes the gate set cannot see
        run: python3 scripts/check-workflow-write-safety.py
      # And the ruleset that pins branch protection against those names (#162).
      # Offline only: it checks that every required context is a real check
      # name, that no branch requires one that cannot report there, and that
      # every PR check is required somewhere or excused by name. The last is
      # what makes adding a gate a decision rather than a default — a new job
      # fails this step until someone says whether it is part of the merge
      # contract. Live verification (--verify) needs admin scope and is run by
      # hand, not here.
      - name: Branch-protection ruleset matches the check names
        run: python3 scripts/check-branch-protection.py
