name: Compliance evidence

on:
  # Evidence must be produced by a branch run that can be reviewed before a
  # release tag is created. The release guard resolves the completed branch
  # run after commit creation; no commit contains its own digest or artifact ID.
  push:
    branches: [main, develop, integration]
  workflow_dispatch:

permissions:
  actions: read
  contents: read

env:
  PYTHONUNBUFFERED: "1"

jobs:
  produce:
    name: Produce control-bound evidence
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - uses: ./.github/actions/setup-uv
      - run: uv sync --locked --extra dev
      - name: Validate source claims before executing requests
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: uv run python scripts/check-compliance.py
      - name: Run registry-declared tests
        run: >-
          uv run python scripts/produce-compliance-evidence.py
          --release-digest "$GITHUB_SHA"
          --output compliance-evidence
      - name: Upload immutable compliance evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: compliance-evidence-${{ github.sha }}
          path: compliance-evidence/
          if-no-files-found: error
          retention-days: 90
      # Do not verify our own run conclusion here: it cannot be completed
      # until this job exits. The release guard verifies it independently.
