# L1120 repair — second pass (job 24111eb) — COMPLETE

## Outcome
Started from merge head 92a366540 (prior pass, job d7faba). Independent
re-validation of every #1120 acceptance criterion confirmed the merged
implementation, and surfaced two real edge defects (both reproduced before
repair, see below). Repairs applied on top and committed.

## Defects found and fixed (with reproduction evidence)
1. Retry-after-exhaustion refusal: a retry carrying the same `fire_id` after
   the winner's fire spent the last `max_runs` unit answered 409
   "has used all 1 of its runs" instead of reconciling to the winner's Run.
   Fixed by probing the occurrence claim *before* any refusal in
   `_admit_manual` (admission.py), removing `_fire_manual_canonical`'s
   stale-ordered local `exhausted` pre-check (scheduler.py), and making the
   reconciliation branch read the durable row for the disable flag so the
   loser's Hive-row projection cannot report an already-disabled schedule as
   enabled.
2. Unbounded `Idempotency-Key` header: a 5,000-character header flowed
   verbatim into durable Run provenance as `schedule_fire_id` (the body's
   `fire_id` is bounded to 200 by `_check_fire_id`; the header path was not).
   The route now holds the header to the same stripped/bounded contract
   (blank header = absent = server-minted identity; >200 chars = 422 before
   any durable write).

## Tests added
- packages/maistro-core/tests/scheduling/test_admission.py:
  `test_a_retry_after_exhaustion_reconciles_to_the_winners_run`,
  `test_a_retry_after_the_template_disappeared_still_reconciles` (+2)
- packages/hive-conductor/backend/tests/test_manual_fire_canonical.py:
  `test_a_retry_after_exhaustation_returns_the_winners_receipt`,
  `test_an_idempotency_key_meets_the_fire_id_contract` (+2)
- inventory note updated (+18/+12 total delta); suite-inventory check green.

## Validation (all green on the committed state)
- ruff check + format --check: clean (2494 files).
- mypy six packages/*/src: Success, 710 files.
- maistro-core runs+scheduling with PG legs (MAISTRO_TEST_PG_DSN, container
  auto-1120-pg port 55441): 1290 passed, 3 skipped (capability-gated only).
- Adjacent consumers (container wiring, schedule winner crash, pg admission):
  43 passed.
- hive-conductor backend (bare python per SUITE-INVENTORY): 2493 passed,
  1 skipped.
- Gates: convergence-matrix, m1-convergence-freeze (--base develop
  0221d2cd), doc-links, suite-inventory, execution-lifecycles,
  lifecycle-provenance, enumerations-provenance: all OK.
- alembic: single head 039; fresh-DB upgrade on PG18 verified;
  ix_canonical_runs_occurrence COALESCE index confirmed via pg_indexes.

## Notes for any successor
- PG container `auto-1120-pg` (port 55441) still running for verification;
  scratch DB `m1120_fresh` was created for the migration check and can be
  dropped.
- `.handoff-1120-merge.md` (prior pass) is superseded by this file; both are
  untracked scratch by design, not part of the commit.
- No pushes/PRs/issue mutations performed (hard prohibition honored).

---

# Third pass (job b18d0705) — independent re-validation — COMPLETE, no new defects

Started at head 4e4598d81. Re-verified the prior findings and every #1120
acceptance criterion by direct execution (no prior claim trusted):

- Legacy SQLite occurrence index (prior finding #1):
  `test_ensure_schema_replaces_the_legacy_occurrence_index` PASSED standalone;
  full runs+scheduling suites green. sqlite_store `_SCHEMA` carries the
  DROP+CREATE COALESCE index; wiring.py calls ensure_schema on the SQLite leg.
- PG leg (container auto-1120-pg, DSN user maistro/test, db maistro,
  port 55441): `packages/maistro-core/tests/runs` + `tests/scheduling` with
  MAISTRO_TEST_PG_DSN -> 1290 passed, 3 skipped (capability-gated only).
  (First attempt failed only on a wrong guessed password; not a code issue.)
- Hive backend full suite (bare venv python per SUITE-INVENTORY):
  2493 passed, 1 skipped. Targeted manual-fire/scheduler files: 90 passed.
  Key E2E rerun individually: test_the_manual_fire_route_runs_the_canonical_spine_end_to_end,
  test_an_idempotency_key_meets_the_fire_id_contract — both PASSED.
- ruff check: clean (2494 files). ruff format --check: clean.
- mypy six packages/*/src: Success, 710 files.
- Gates OK: convergence-matrix, doc-links, m1-convergence-freeze
  (--base 6f460442), execution-lifecycles, lifecycle-provenance,
  enumerations-provenance, suite-inventory (13 suites).
- Alembic: single head 039. Fresh-DB upgrade on PG18 (scratch m1120_verify)
  applied 000->039; `ix_canonical_runs_occurrence` COALESCE('manual:'||fire_id,
  scheduled_for) unique partial index confirmed via pg_indexes; scratch DBs
  m1120_verify and the prior pass's m1120_fresh dropped.

Verdict: MERGE-READY. No code changes needed this pass; tree left exactly at
committed head 4e4598d81 (clean except this untracked scratch note, per the
established convention for these handoff files).

---

# Fourth pass (job 062bc629) — develop re-merge 84d937add — COMPLETE

Started at head 4e4598d81 (prior pass's MERGE-READY state). The driver moved
the develop base to 84d937add (14 new commits incl. #1531/#1079/#1395/#1533),
so the branch re-merged develop:

- alembic collision: our 039_manual_fire_occurrence_identity renumbered
  039->041->042 and re-parented onto develop's real tip 036_audit_log_org_scope
  (single head 042; `alembic heads`/`history` verified linear).
- runs/store.py conflict resolved keeping BOTH develop's
  _claim_occurrence/_claim_canvas_job (#1055) and our find_occurrence_run
  (#220/#1120); imports take Mapping+Sequence.
- tests/migrations/test_audit_scope_migration.py::test_audit_scope_migration_is_the_single_head
  re-pinned to the collision-proof form (one head, audit migration on its
  chain) — 042 extends the chain exactly as 036_audit_log_org_scope itself
  re-parented onto 040.
- admission.py auto-merge verified semantically: full delta vs develop is
  exactly our #1120 additions; develop's #1533 recovery work intact; our
  settle_fire/reserve_fire call sites match develop's protocol.

Validation (all on committed head 539bf5f54): core runs+scheduling PG legs
1322 passed/3 skipped on FRESH DB m1120_r4 (fresh `alembic upgrade head` to
042; canvas-job index + COALESCE('manual:'||fire_id, scheduled_for)
occurrence index verified via pg_indexes); tests/migrations 96 passed; Hive
backend 2577 passed/1 skipped (targeted manual-fire/scheduler files 80
passed; six key E2E tests re-run individually: 5+1 passed — the sixth,
retry-after-exhaustion service-level, is in the 80). NOTE: tests/migrations
cycles the scratch schema; re-run `alembic upgrade head` on m1120_r4 before
any later PG-leg suite. ruff+format clean; mypy 711 files Success; gates:
convergence-matrix, doc-links, suite-inventory, execution-lifecycles,
lifecycle-provenance, enumerations-provenance, merge-markers,
m1-convergence-freeze (--base 84d937add) all OK.

No pushes/PRs/issue mutations. Verdict: MERGE-READY.

---

# Fifth pass (job 72bd508d) — independent repair-phase re-validation — COMPLETE, no defects

Started at head 539bf5f54c235e0de2319a2e5ab80c8849c520b5 (fourth pass's MERGE-READY
state; the intervening job b4a1d766 died immediately on a provider 429 and
made no changes). No driver check-*.log existed for this job (manifest
`checks: []`), so every criterion was re-validated by direct execution:

- Prior finding #1 (legacy SQLite occurrence index): re-verified in code
  (`_SCHEMA` DROPs then recreates `idx_canonical_runs_occurrence` with the
  COALESCE('manual:'||fire_id, scheduled_for) expression) and by running
  `test_ensure_schema_replaces_the_legacy_occurrence_index` standalone —
  PASSED.
- Prior finding #2 (wiring reachability): `wiring.py` SQLite leg calls
  `ensure_schema()` on the run store (line 249); SQLite-leg runs+scheduling
  suites green.
- PG legs: first attempt on the reused container DB `maistro` produced 2
  canvas-job failures — root-caused to stale schema on that DB (alembic
  stamped at our pre-renumber "039", missing develop's 039 canvas-job
  index), not a code defect. FRESH scratch DB `m1120_r5` created on
  auto-1120-pg; `alembic upgrade head` applied the chain linearly to single
  head 042; runs+scheduling then: 1322 passed, 3 skipped.
- tests/migrations: 29 passed, 67 skipped (PG legs against m1120_r5).
- Hive backend full suite (bare .venv python per SUITE-INVENTORY): 2577
  passed, 1 skipped. Targeted manual-fire/scheduler files: 80 passed.
  `TestManualFire` admission class: 11 passed. Spine conformance manual-fire
  subset (SQLite+PG legs): 12 passed.
- ruff check clean (2511 files); ruff format --check clean; mypy six
  packages/*/src: Success, 711 files.
- Gates OK: convergence-matrix, doc-links, suite-inventory (13 suites),
  execution-lifecycles, lifecycle-provenance, enumerations-provenance,
  merge-markers, m1-convergence-freeze (--base 84d937add).
- All twelve #1120 acceptance criteria verified against code + executed
  tests (canonical admission spine, canonical Workspace/Project scope,
  fire_id occurrence identity, real-route double-submit reconciliation,
  manual-vs-recurring provenance, no run_registered_dag authority, durable
  template store, prompt consumption, failure ordering, gated standalone
  fallback, docs/matrix accuracy, real-route E2E).

No code changes were required this pass; the only artifact is this record
(committed locally per the writer rule). Scratch DB `m1120_r5` on
auto-1120-pg can be dropped. No pushes/PRs/issue mutations.

Verdict: MERGE-READY.

---

# Sixth pass (job cf5468af) — crash-window repair: the hold spends nothing — COMPLETE

Started at head d60bc7fd6 with uncommitted salvage from a killed job already in
the tree (backed up to ~/Git/wt/auto-1120/../incoming-1120.patch before any
further work). The salvage is the repair for the audit finding that
`reserve_fire` durably counted the run and disabled on exhaustion *before* any
Run existed (admission.py reserve-before-admit; store.py increment/disable):
a fresh-SQLite crash reproduction preserved runs_so_far=1, enabled=False,
last_run_id=None — a max_runs=1 schedule bricked with no Run. The salvage
replaces the in-memory FireReservation with a durable PendingFire marker:
the hold counts against exhaustion (race safety #1119 kept) but spends
nothing; runs_so_far/enabled/last_run_id land only in settle_pending_fire,
the one write that also removes the marker and links the Run. Recovery
(_reconcile_pending_fires) settles lease-expired markers against the Run
store: a Run for the token confirms the spend, none releases the slot; a
fresh marker is untouchable. Both store implementations (in-memory, PG) share
_reserve/_settle_pending; pg_store updated; admission._admit_manual
reconciles markers before anything else; recurring admit_due reconciles when
the snapshot holds markers; ScheduleAdmission.disabled now reports only this
fire's own settle. scheduler.py's committed CRLF corruption (merge d60bc7fd6)
normalized back to LF (content byte-identical, verified
--ignore-cr-at-eol); handoff trailing space fixed.

Validation (all executed this pass on the committed-to-be state):
- ruff check + format --check on changed paths: clean. git diff --check: 0.
- mypy six packages/*/src: Success, 711 files.
- maistro-core tests/scheduling: 239 passed/32 skipped (SQLite legs);
  with MAISTRO_TEST_PG_DSN on fresh pgvector:pg18 container
  auto-1120-repair3-pg (port 55443, alembic upgrade head -> 042): 271 passed.
- maistro-core tests/runs (PG legs): 1064 passed, 3 skipped.
- Hive backend full suite (bare .venv python per SUITE-INVENTORY):
  2577 passed, 1 skipped. Targeted manual-fire/scheduler files: 90 passed.
- Key E2E re-runs individually, all PASSED:
  test_the_manual_fire_route_runs_the_canonical_spine_end_to_end,
  test_an_idempotency_key_meets_the_fire_id_contract,
  test_a_double_submit_reconciles_to_one_run,
  test_the_standalone_fallback_is_gated_to_no_container.
- Gates (venv python where structlog needed): suite-inventory 13/13 ok after
  recording +13 maistro-core delta in the 1120 inventory note (store +9/+1,
  admission +3); convergence-matrix OK; doc-links OK; execution-lifecycles OK;
  lifecycle-provenance OK (0 violations); enumerations-provenance OK;
  merge-markers OK; m1-convergence-freeze --base 8bb344e32 OK.
- Code spot-checks (not trusting prior passes): fire_now() enters
  _fire_manual_canonical via _canonical_admitter when a Container exists,
  raises ScheduleAdmissionUnavailable when a Container lacks admission
  collaborators (fail closed), and only a Container-less process reaches
  run_registered_dag; hive:schedule:{sid} survives only inside
  _as_definition/_fire_schedule compatibility code; CONVERGENCE-MATRIX row
  128 and summary 181 describe recurring+manual canonical admission with
  run_registered_dag as the no-Container fallback only.

No pushes/PRs/issue mutations. Scratch container auto-1120-repair3-pg can be
removed. Verdict: MERGE-READY.

---

# Seventh pass (job 52923878) — develop re-merge 8bb344e32 — COMPLETE

Started at head 0124d8b6f with the driver's in-progress `git merge 8bb344e32`
(the develop base moved; no check-*.log provided, manifest `checks: []`). The
merge conflicted on `routes/schedules.py` where develop's #1201 (workspace-
scoped route authority: `_actor`/`_visible_schedule`/`_require_writer`/
`_writable_schedule`, `Request`) crosses our #1120 (`Header`/`Idempotency-Key`,
`_check_fire_id`, `ManualFireBody`). Resolved as a semantic union on merge-base
411a21856; merged `run_schedule` takes request + body + Idempotency-Key so a
manual fire is both workspace-scoped and stably identified. Committed as
5ea1c5179. The merge surfaced two real integration breaks, both fixed and
committed (1c33d19b4):

- our route E2Es posted as the seeded admin without canonical Workspace
  membership -> 404 under #1201; they now seed the canonical Workspace
  (`canonical_store_for_tests().create(creator_user_id="admin", ...,
  workspace_id=...)`, owner via creator membership),
- develop's `fire_then_lose_the_row` stub lacked the `fire_id` keyword the
  #1120 route passes -> TypeError; stub signature updated.

Validation (all executed this pass on committed heads 5ea1c5179/1c33d19b4):
- targeted manual-fire/scheduler/scope files: 107 passed;
- hive backend full suite (bare .venv python per SUITE-INVENTORY):
  2665 passed, 1 skipped;
- maistro-core scheduling+runs+workspaces: 1205 passed, 279 skipped (PG legs
  capability-gated; PG-leg parity proven on fresh DBs in passes 4-6);
- ruff check clean; ruff format --check clean (1 file reformatted);
  git diff --check 0 (base..HEAD range too);
- mypy six packages/*/src: Success, 712 files;
- gates: suite-inventory 13/13, convergence-matrix, doc-links, merge-markers,
  execution-lifecycles, lifecycle-provenance (0 violations),
  enumerations-provenance, m1-convergence-freeze (--base 8bb344e32) all OK.
- Post-merge spot checks: fire_now gates on _canonical_admitter ->
  _fire_manual_canonical (scheduler.py:92-99); ScheduleAdmissionUnavailable
  fail-closed for a mis-wired Container (:100-103); run_registered_dag +
  hive:schedule:{sid} only in the no-Container compat path (:818/834/847);
  CONVERGENCE-MATRIX 128/181 describe recurring+manual canonical admission
  with run_registered_dag as no-Container fallback only.

Verdict: MERGE-READY. No pushes/PRs/issue mutations.

---

# Eighth pass (job 6b3ac7d5) — repair-phase re-validation after develop re-merge 1dea30dfe — COMPLETE, no code changes

Started at head 6015d1a50c (driver's merge of develop 1dea30dfe into the
branch; no check-*.log in this job dir — driver checks for this head live in
prior job 8a481ff0's logs, all returncode 0). Everything re-validated by
direct execution:

- Prior finding "radon EXIT=1 at HEAD (admission.py:946 _admit_manual C(14)
  unbaselined)": RESOLVED at this head. scripts/check-radon-baseline.py
  EXIT=0 (70 current C/D/E/F blocks, 70 baseline entries, 0 new / 0 regressed
  / 0 improved / 0 stale). Root cause of resolution: the merged develop
  already carries _admit_manual in its trusted-base oracle, and the merged
  branch implementation refactored the function to radon rank B (complexity
  9, verified via radon cc) — no baseline edit was made (quality/ is
  byte-identical base..HEAD).
- Prior finding "vulture EXIT=1, branch adds 6 un-banked identities":
  OBSOLETE after the re-merge. vulture scans of the merge base (git archive
  1dea30dfe scratch tree) and HEAD produce the IDENTICAL identity multiset
  (1430 findings each; 0 added / 0 removed, line-number-only diffs), and
  quality/vulture-baseline.json + ratchet-authorizations.json are identical
  base..HEAD — so the gate failure (1430 vs 1415 reviewed) is 100% upstream
  trusted-base staleness, reproduced at develop tip itself. The remedy — a
  reviewed grant in quality/ratchet-authorizations.json — is explicitly out
  of scope for this lane (hard prohibition: no ledger/grant edits).
  Residual: gate red upstream and here until the grant authority lands it.
- ruff check + ruff format --check: clean.
- maistro-core tests/scheduling + runs/test_spine_conformance.py +
  runs/test_sqlite_store.py (SQLite legs): 516 passed, 150 skipped
  (capability-gated PG legs; PG-leg parity proven on fresh DBs in passes
  4-6 and by prior job's check-3 at this head).
- Hive manual-fire/scheduler/scope suites (bare .venv python per
  SUITE-INVENTORY): 100 passed.
- Key E2Es re-run individually, all PASSED:
  test_the_manual_fire_route_runs_the_canonical_spine_end_to_end (real
  route + real Container + Idempotency-Key double submit; asserts one Run in
  canonical Workspace/Project scope, schedule_trigger=manual +
  schedule_fire_id provenance, prompt consumption to completed NodeRun +
  Attempt, runs_so_far==1),
  test_a_double_submit_reconciles_to_one_run,
  test_an_idempotency_key_meets_the_fire_id_contract,
  test_the_standalone_fallback_is_gated_to_no_container,
  test_a_retry_after_exhaustation_returns_the_winners_receipt.
- tests/migrations/test_audit_scope_migration.py: 3 passed.
- Gates: check-convergence-matrix OK (52 subsystems / 1117 modules),
  check-doc-links OK, check-suite-inventory OK for both touched suites.
- Code spot checks: fire_now gates on _canonical_admitter ->
  _fire_manual_canonical (scheduler.py:92-99); Container without admission
  wiring raises ScheduleAdmissionUnavailable (fail closed, :100-103);
  run_registered_dag + synthetic hive:schedule:{sid} only in the no-Container
  compat path (:229-230 _as_definition, :834 _fire_schedule);
  CONVERGENCE-MATRIX row 128 + summary 181 describe recurring+manual
  canonical admission with run_registered_dag as the no-Container fallback
  only.

No code changes required this pass; the only artifact is this record.
No pushes/PRs/issue mutations. Verdict: MERGE-READY (writer handoff only).

---

# Ninth pass (job b566e751) — CI-repair: vulture ratchet green + develop re-merge 2c8022fe8 — COMPLETE

Started at head 4632f284b (clean tree; prior job 541c177b died on a provider
timeout with zero checks executed, so nothing was trusted and everything was
re-executed).

## Repair (commit 72eb41d58)
The canonical CI invocation `check-vulture-baseline.py packages/*/src
--min-confidence 60 --exclude '*/third_party/*'` failed at HEAD with exactly
one branch-caused delta pair:
1. NEW unbanked identity: `admission.py:372 reconciled_run_id` — NOT dead: its
   only reader is hive-conductor/backend/services/scheduler.py:555, outside
   the vulture scan scope (packages/*/src), and tests exercise it. A candidate
   ledger bank cannot self-authorize new debt (grants are read from the merge
   base — ratchet_provenance.load_authorizations), so the field declaration
   carries vulture's own coded per-finding suppression (# noqa: V107) with the
   rationale in its contract docstring. quality/ untouched.
2. `tasks/lanes.py::unused method 'held'` was at risk of going dark: #1120's
   own locals named `held` (scheduling/model.py exhausted/runs_remaining,
   scheduling/store.py _reserve) made vulture's name-based analysis count the
   real, banked debt as used. Renamed those locals to `pending_holds`
   (behavior-neutral) so the genuine finding is reported again and the ledger
   identity stays valid without pruning.

## Develop re-merge (commit f075f759c)
Manifest base moved to 2c8022fe8 (12 commits beyond the merged 60862b6c5).
Only overlapping file was docs/architecture/CONVERGENCE-MATRIX.md (auto-merged
cleanly; row 128 + summary 181 intact). No conflicts. alembic: develop added
no revisions; single head 042 stands.

## Validation (all executed this pass, post-merge, head f075f759c)
- Gates: vulture EXIT=0 (1415==1415 vs base 2c8022fe8), radon EXIT=0 (69/69),
  convergence-matrix OK (52 subsystems / 1122 modules), suite-inventory OK
  (13 suites), doc-links OK, m1-convergence-freeze --base 2c8022fe OK.
- ruff check clean; ruff format --check clean (2547 files); six-package mypy
  Success (713 files) after `uv sync --extra bootstrap` (the 5 pre-existing
  import-not-found errors were the missing optional extra, not branch code).
- maistro-core tests/scheduling: 247 passed/34 skipped (SQLite); with PG legs
  on FRESH pg18 container auto-1120-r6-pg (port 55445, alembic upgrade head
  000->042 linear, ix_canonical_runs_occurrence COALESCE('manual:'||fire_id,
  scheduled_for) unique partial index verified via pg_indexes): 281 passed/0
  skipped. tests/runs: 269 passed/116 skipped (SQLite); PG legs 1082
  passed/3 skipped (capability-gated) — includes spine-conformance postgres
  legs that had failed environmentally on the stale shared DB (prior finding
  re-proven environmental: fresh DB is fully green).
- Hive backend (bare .venv python per SUITE-INVENTORY): full suite 2730
  passed/6 skipped post-merge; targeted manual-fire/scheduler/scope files 100
  passed; flagship E2E
  test_the_manual_fire_route_runs_the_canonical_spine_end_to_end re-run
  individually PASSED (real route + real Container + Idempotency-Key double
  submit -> one canonical Run in Workspace/Project scope with
  schedule_trigger=manual + schedule_fire_id provenance, prompt consumed to
  completed NodeRun/Attempt).
- tests/migrations/test_audit_scope_migration.py: 3 passed (PG DSN).
- Scratch container auto-1120-r6-pg removed after the run.

No pushes/PRs/issue mutations. Verdict: MERGE-READY (writer handoff only).
