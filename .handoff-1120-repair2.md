# L1120 repair — second pass (job 24111eb) — COMPLETE

## Outcome
Started from merge head 92a366540 (prior pass, job d7faba). Independent
re-validation of every #1120 acceptance criterion confirmed the merged
implementation, and surfaced two real edge defects (both reproduced before
repair, see below). Repairs applied on top and committed.

## Defects found and fixed (with reproduction evidence)
1. Retry-after-exhaustion refusal: a retry carrying the same `fire_id` after
   the winner's fire spent the last `max_runs` unit answered 409
   "has used all 1 of its runs" instead of reconciling to the winner's Run.
   Fixed by probing the occurrence claim *before* any refusal in
   `_admit_manual` (admission.py), removing `_fire_manual_canonical`'s
   stale-ordered local `exhausted` pre-check (scheduler.py), and making the
   reconciliation branch read the durable row for the disable flag so the
   loser's Hive-row projection cannot report an already-disabled schedule as
   enabled.
2. Unbounded `Idempotency-Key` header: a 5,000-character header flowed
   verbatim into durable Run provenance as `schedule_fire_id` (the body's
   `fire_id` is bounded to 200 by `_check_fire_id`; the header path was not).
   The route now holds the header to the same stripped/bounded contract
   (blank header = absent = server-minted identity; >200 chars = 422 before
   any durable write).

## Tests added
- packages/maistro-core/tests/scheduling/test_admission.py:
  `test_a_retry_after_exhaustion_reconciles_to_the_winners_run`,
  `test_a_retry_after_the_template_disappeared_still_reconciles` (+2)
- packages/hive-conductor/backend/tests/test_manual_fire_canonical.py:
  `test_a_retry_after_exhaustation_returns_the_winners_receipt`,
  `test_an_idempotency_key_meets_the_fire_id_contract` (+2)
- inventory note updated (+18/+12 total delta); suite-inventory check green.

## Validation (all green on the committed state)
- ruff check + format --check: clean (2494 files).
- mypy six packages/*/src: Success, 710 files.
- maistro-core runs+scheduling with PG legs (MAISTRO_TEST_PG_DSN, container
  auto-1120-pg port 55441): 1290 passed, 3 skipped (capability-gated only).
- Adjacent consumers (container wiring, schedule winner crash, pg admission):
  43 passed.
- hive-conductor backend (bare python per SUITE-INVENTORY): 2493 passed,
  1 skipped.
- Gates: convergence-matrix, m1-convergence-freeze (--base develop
  0221d2cd), doc-links, suite-inventory, execution-lifecycles,
  lifecycle-provenance, enumerations-provenance: all OK.
- alembic: single head 039; fresh-DB upgrade on PG18 verified;
  ix_canonical_runs_occurrence COALESCE index confirmed via pg_indexes.

## Notes for any successor
- PG container `auto-1120-pg` (port 55441) still running for verification;
  scratch DB `m1120_fresh` was created for the migration check and can be
  dropped.
- `.handoff-1120-merge.md` (prior pass) is superseded by this file; both are
  untracked scratch by design, not part of the commit.
- No pushes/PRs/issue mutations performed (hard prohibition honored).

---

# Third pass (job b18d0705) — independent re-validation — COMPLETE, no new defects

Started at head 4e4598d81. Re-verified the prior findings and every #1120
acceptance criterion by direct execution (no prior claim trusted):

- Legacy SQLite occurrence index (prior finding #1): 
  `test_ensure_schema_replaces_the_legacy_occurrence_index` PASSED standalone;
  full runs+scheduling suites green. sqlite_store `_SCHEMA` carries the
  DROP+CREATE COALESCE index; wiring.py calls ensure_schema on the SQLite leg.
- PG leg (container auto-1120-pg, DSN user maistro/test, db maistro,
  port 55441): `packages/maistro-core/tests/runs` + `tests/scheduling` with
  MAISTRO_TEST_PG_DSN -> 1290 passed, 3 skipped (capability-gated only).
  (First attempt failed only on a wrong guessed password; not a code issue.)
- Hive backend full suite (bare venv python per SUITE-INVENTORY):
  2493 passed, 1 skipped. Targeted manual-fire/scheduler files: 90 passed.
  Key E2E rerun individually: test_the_manual_fire_route_runs_the_canonical_spine_end_to_end,
  test_an_idempotency_key_meets_the_fire_id_contract — both PASSED.
- ruff check: clean (2494 files). ruff format --check: clean.
- mypy six packages/*/src: Success, 710 files.
- Gates OK: convergence-matrix, doc-links, m1-convergence-freeze
  (--base 6f460442), execution-lifecycles, lifecycle-provenance,
  enumerations-provenance, suite-inventory (13 suites).
- Alembic: single head 039. Fresh-DB upgrade on PG18 (scratch m1120_verify)
  applied 000->039; `ix_canonical_runs_occurrence` COALESCE('manual:'||fire_id,
  scheduled_for) unique partial index confirmed via pg_indexes; scratch DBs
  m1120_verify and the prior pass's m1120_fresh dropped.

Verdict: MERGE-READY. No code changes needed this pass; tree left exactly at
committed head 4e4598d81 (clean except this untracked scratch note, per the
established convention for these handoff files).

---

# Fourth pass (job 062bc629) — develop re-merge 84d937add — COMPLETE

Started at head 4e4598d81 (prior pass's MERGE-READY state). The driver moved
the develop base to 84d937add (14 new commits incl. #1531/#1079/#1395/#1533),
so the branch re-merged develop:

- alembic collision: our 039_manual_fire_occurrence_identity renumbered
  039->041->042 and re-parented onto develop's real tip 036_audit_log_org_scope
  (single head 042; `alembic heads`/`history` verified linear).
- runs/store.py conflict resolved keeping BOTH develop's
  _claim_occurrence/_claim_canvas_job (#1055) and our find_occurrence_run
  (#220/#1120); imports take Mapping+Sequence.
- tests/migrations/test_audit_scope_migration.py::test_audit_scope_migration_is_the_single_head
  re-pinned to the collision-proof form (one head, audit migration on its
  chain) — 042 extends the chain exactly as 036_audit_log_org_scope itself
  re-parented onto 040.
- admission.py auto-merge verified semantically: full delta vs develop is
  exactly our #1120 additions; develop's #1533 recovery work intact; our
  settle_fire/reserve_fire call sites match develop's protocol.

Validation (all on committed head 539bf5f54): core runs+scheduling PG legs
1322 passed/3 skipped on FRESH DB m1120_r4 (fresh `alembic upgrade head` to
042; canvas-job index + COALESCE('manual:'||fire_id, scheduled_for)
occurrence index verified via pg_indexes); tests/migrations 96 passed; Hive
backend 2577 passed/1 skipped (targeted manual-fire/scheduler files 80
passed; six key E2E tests re-run individually: 5+1 passed — the sixth,
retry-after-exhaustion service-level, is in the 80). NOTE: tests/migrations
cycles the scratch schema; re-run `alembic upgrade head` on m1120_r4 before
any later PG-leg suite. ruff+format clean; mypy 711 files Success; gates:
convergence-matrix, doc-links, suite-inventory, execution-lifecycles,
lifecycle-provenance, enumerations-provenance, merge-markers,
m1-convergence-freeze (--base 84d937add) all OK.

No pushes/PRs/issue mutations. Verdict: MERGE-READY.

---

# Fifth pass (job 72bd508d) — independent repair-phase re-validation — COMPLETE, no defects

Started at head 539bf5f54c235e0de2319a2e5ab80c8849c520b5 (fourth pass's MERGE-READY
state; the intervening job b4a1d766 died immediately on a provider 429 and
made no changes). No driver check-*.log existed for this job (manifest
`checks: []`), so every criterion was re-validated by direct execution:

- Prior finding #1 (legacy SQLite occurrence index): re-verified in code
  (`_SCHEMA` DROPs then recreates `idx_canonical_runs_occurrence` with the
  COALESCE('manual:'||fire_id, scheduled_for) expression) and by running
  `test_ensure_schema_replaces_the_legacy_occurrence_index` standalone —
  PASSED.
- Prior finding #2 (wiring reachability): `wiring.py` SQLite leg calls
  `ensure_schema()` on the run store (line 249); SQLite-leg runs+scheduling
  suites green.
- PG legs: first attempt on the reused container DB `maistro` produced 2
  canvas-job failures — root-caused to stale schema on that DB (alembic
  stamped at our pre-renumber "039", missing develop's 039 canvas-job
  index), not a code defect. FRESH scratch DB `m1120_r5` created on
  auto-1120-pg; `alembic upgrade head` applied the chain linearly to single
  head 042; runs+scheduling then: 1322 passed, 3 skipped.
- tests/migrations: 29 passed, 67 skipped (PG legs against m1120_r5).
- Hive backend full suite (bare .venv python per SUITE-INVENTORY): 2577
  passed, 1 skipped. Targeted manual-fire/scheduler files: 80 passed.
  `TestManualFire` admission class: 11 passed. Spine conformance manual-fire
  subset (SQLite+PG legs): 12 passed.
- ruff check clean (2511 files); ruff format --check clean; mypy six
  packages/*/src: Success, 711 files.
- Gates OK: convergence-matrix, doc-links, suite-inventory (13 suites),
  execution-lifecycles, lifecycle-provenance, enumerations-provenance,
  merge-markers, m1-convergence-freeze (--base 84d937add).
- All twelve #1120 acceptance criteria verified against code + executed
  tests (canonical admission spine, canonical Workspace/Project scope,
  fire_id occurrence identity, real-route double-submit reconciliation,
  manual-vs-recurring provenance, no run_registered_dag authority, durable
  template store, prompt consumption, failure ordering, gated standalone
  fallback, docs/matrix accuracy, real-route E2E).

No code changes were required this pass; the only artifact is this record
(committed locally per the writer rule). Scratch DB `m1120_r5` on
auto-1120-pg can be dropped. No pushes/PRs/issue mutations.

Verdict: MERGE-READY.
