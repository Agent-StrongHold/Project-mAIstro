Merge queue passing canary.

Purpose: verify that a harmless PR entering the develop merge queue receives the full required check set on its synthetic merge-group SHA and lands only after those checks succeed.
