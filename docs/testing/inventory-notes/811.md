---
inventory-delta:
  packages/maistro-bootstrap/tests: +38
---

# 811

`run_tests` (the builders agent-loop tool) appended model-provided pytest
args verbatim, so `-p evil_plugin` could load a module of the model's
choosing ahead of the `run_command` approval gate. #811 adds an explicit
pytest-flag allowlist enforced on shlex-parsed argv tokens.

All +38 node IDs are one new suite file,
`packages/maistro-bootstrap/tests/test_agent_loop_run_tests_args.py`:
- 10 parametrized extension-loading vectors (`-p`, `-c`, `--config-file`,
  `--pyargs`, `--override-ini`, `--import-mode`, glued/long spellings) proven
  to never reach `sandbox.run_argv`;
- 15 parametrized parity cases proving ordinary selection/verbosity flags
  (`-k`, `-m`, `-x`, `--maxfail`, `--tb`, `--ignore`, `--deselect`, `--lf`,
  `-q/-v`, node ids, quoted expressions) still arrive byte-for-byte;
- the remainder: all-or-nothing rejection, parsed-token (not substring)
  semantics, malformed/unparseable input rejection, and the omitted/null
  `args` backward-compatibility path.

No other suite moved. Note for local runs: `check-suite-inventory.py`
without `--suite` also fails to collect the two hive-conductor suites in
this environment; that failure predates this branch (reproduces on a clean
tree) and is unrelated to this delta.
