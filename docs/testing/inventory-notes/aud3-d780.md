---
inventory-delta:
  packages/hive-conductor/backend/tests: +2
---
# aud3-d780

The #1239 elevation regression suite adds two cases: an elevation cannot
bind to a foreign/terminal/unknown task, and a configured TTL cannot exceed
one hour (including a runtime cap when settings are bypassed). Existing
fixture helpers now create explicitly owned active task records so their
successful elevation cases exercise the same authoritative binding contract.
