---
inventory-delta:
  packages/maistro-core/tests: +4
---
# auto-1058-1e7d

Adds scoped HITL expiry regressions proving effective-principal evidence
settles only the authorized Workspace, refuses a mismatched mutation scope,
and skips malformed deadline projections. Adds backend coverage proving every
HITL settlement rejects an omitted object-authorization proof before target
lookup.
