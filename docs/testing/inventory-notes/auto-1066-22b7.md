---
inventory-delta:
  packages/hive-conductor/backend/tests: +4
---
# auto-1066-22b7

Four new tests in `packages/hive-conductor/backend/tests/test_evolution_service.py`
for the Codex review finding on #1300: execution availability now includes
domain readiness. A healthy engine with no population/tournament reports
`execution_available: false`, `running: false` and an
"Evolution population is not initialized" reason; a domain init that fails
keeps execution unavailable, carries the init error in the reason, and makes
`_run_one_cycle` raise `EvolutionUnavailableError` (a 503) rather than a bare
`RuntimeError`; a successful domain init turns execution on; and
`start_evolution` schedules no cadence when the domain state could not be
built. Nothing was removed or moved.
