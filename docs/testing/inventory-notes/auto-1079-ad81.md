---
inventory-delta:
  packages/maistro-core/tests: +7
---
# auto-1079-ad81

Repair pass on the #1079 model-egress composition lane. This session added
eleven collected tests to `packages/maistro-core/tests`: six in the new
`tests/config/test_model_binding_config.py` (YAML pass-through of declared
model Bindings plus `AgentConfig`-boundary validation of blank identity/scope
and empty refs) and two consumer-tick composition tests in
`tests/test_model_egress_container_composition.py` (the Container's own
`execute_admitted_runs` completing a configured summarize Run end to end, and
its twin failing closed with the registration step removed), on top of the
five this branch had already added there. One test was added to
`packages/maistro-server/tests` file `api/test_conductor_agent.py` (two cases:
`_agent_config` maps `model_bindings` from `MaistroYamlConfig`; no YAML
authorizes nothing) and one to `packages/hive-conductor/backend/tests`
(`test_dag_agents.py`: the Hive node-resolver bridge wires `llm.summarize`
with the Container's own effect context, Provider registry, and router).

The composition file's five tests are counted under
`pr-1079-model-egress-composition.md`; this note counts this session's nine
other core tests (six config-boundary, two consumer-tick, one synth-dag
child-resolver identity) as +7 because
the pre-session ledger was already two below live collection (a
pre-existing under-recording in some earlier note's aggregate, visible as the
+4 drift this session's first inventory run reported against this branch's
own committed tests, not a change this session made). Folding it here keeps
expected == collected without editing any other lane's shared record.
The server and hive suites matched exactly and are recorded in the lane note.
