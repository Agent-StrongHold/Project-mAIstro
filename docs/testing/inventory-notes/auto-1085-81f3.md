---
inventory-delta:
  packages/hive-conductor/backend/tests: +3
---
# Auto-1085 salvage: production binding provisioning and honest isolation refusal

Second repair round for #1085 (post develop-merge salvage). Net +3 collected
node IDs in the Hive backend suite:

- `test_legacy_dag_node.py`: the two isolation-echo tests are replaced by
  fail-closed pins — an unwired sandbox-classified node raises instead of
  returning the echo script's task text as success, a gateway-configured but
  unwired node refuses raw dispatch with a truthful message, and neither path
  may run the compatibility subprocess (net 0);
- `test_legacy_dag_node.py`: +1 — a stored node with no explicit `binding_id`
  resolves the operator-declared deployment default through the canonical
  Binding store and records the Attempt-correlated Invocation;
- `test_maistro_core_adapter.py`: +2 — the production bridge composes a real
  Container and provisions `Settings.maistro_model_binding_id` into the
  canonical Binding store scoped to the default Workspace's Root Project
  (and an empty declaration provisions nothing, leaving nodes to fail closed).
