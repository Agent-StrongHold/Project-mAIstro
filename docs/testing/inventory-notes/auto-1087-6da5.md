---
inventory-delta:
  packages/hive-conductor/backend/tests: +4
---
# auto-1087-6da5

Reconciled after merging develop's canonical #1064/#1065/#1066 Evolve replay,
availability, and recovery machinery (#1534): that machinery (process-local
cycle markers with fail-closed `FinalizeReconciliationRequired`, frozen cycle
membership, `services.evolution_recovery` cadence) supersedes this branch's
parallel durable operation-marker/snapshot-rollback design, whose tests were
retired with it. On top of the canonical machinery this issue adds, in
`packages/hive-conductor/backend/tests`:

- `test_build_llm_call_uses_canonical_egress_and_correlates_invocation`
  (replaces develop's raw `shared_client` POST test 1:1) — the shipped
  adapter's model selection/shaping parity now holds through the governed
  Capability -> Binding -> Invocation path.
- `test_run_one_cycle_shipped_path_records_governed_invocation` — the
  integration entry through `_EvolutionService._run_one_cycle` proving
  Invocation creation without hand-built Provider/Binding state.
- `test_failed_model_effect_does_not_publish_evaluation_mutation` — a
  swallowed provider timeout cannot fold or publish evaluation scores.
- `test_finalize_swallowed_model_failure_records_faulted_not_committed` —
  finalization over failed physical model work records a `faulted` marker
  and fails closed on replay instead of committing.
- `test_replayed_battle_and_finalize_nodes_do_not_repeat_domain_mutations`
  — compact replay idempotency over the merged `_finalize_cycle(ctx)` shape.

`packages/maistro-evolve/tests` needed no delta: develop's
`TestCycleMarkers`/`TestBattleIdempotency` suites already cover the
population/tournament replay surfaces this branch previously added its own
duplicates for.
