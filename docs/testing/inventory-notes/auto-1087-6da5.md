---
inventory-delta:
  packages/hive-conductor/backend/tests: +4
  packages/maistro-evolve/tests: +2
---
# auto-1087-6da5

The shipped Evolve cycle integration coverage now enters `_EvolutionService._run_one_cycle`,
executes the canonical Graph/Run/NodeRun/Attempt path, and verifies its model work is
recorded as a governed Invocation with execution correlation. Failure and replay-path
tests also prove swallowed provider errors cannot publish scores or repeat domain mutations.

Durable Evolve operation-marker tests cover population finalization evidence and tournament
battle idempotency across a real SQLite close/reopen. The interrupted-finalization recovery
test drives the `started`-marker branch over a real SQLite `PopulationStore`: a crash
mid-finalization rolls back partial breeding from the persisted snapshot, re-runs cull and
breed exactly once over the restored state, and leaves a `committed` marker so replay of the
same logical NodeRun returns the committed output without a second domain mutation pass.
