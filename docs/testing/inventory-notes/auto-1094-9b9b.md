---
inventory-delta:
  packages/hive-conductor/backend/tests: +17
---
# auto-1094-9b9b

The #1094 cases live in `packages/hive-conductor/backend/tests` and cover
nested mapping-key scans, exact serialized result scanning, privileged workflow
classification, canonical inbox approval through the reachable chat route,
exact workflow argument binding, authenticated approval actors, and governed
legacy-node effects (including standalone canonical execution). The repair adds
typed workflow authority, approver-policy, and unavailable-capability audit
regressions. No root `tests/` files changed in this issue.

## Repair-session validation (merge-forward salvage, 2026-09-23)

The develop merge-forward conflicted only on the `routes/chat.py` import block;
resolved by keeping both sides (workflow approval executor + brief-chat turn).
Validation evidence on the merged tree:

- Mutation A (mapping-key scan yield removed from `_text_leaves`):
  `test_scan_config_visits_nested_airtable_field_name_keys` FAILED, proving the
  key-scan regression bites. File restored byte-identical (`git diff` empty).
- Mutation B (`run_workflow` reclassified `TOOL_EFFECT_NETWORK`):
  `test_run_workflow_is_privileged_and_requires_scoped_approval` FAILED, proving
  the privileged-classification regression bites. File restored byte-identical.
- Post-restore: ruff check/format green, mypy maistro-core shows only the five
  pre-existing `import-not-found` errors for the absent optional
  `maistro_bootstrap` extra, and the five-file targeted suite passed 105/105.
- `scripts/check-gates-ran.py` remains an integration-time gate (needs GitHub
  check-run data); its local exit 2 without `--check-runs` is expected, not a
  code defect.

## Independent verifier re-execution (head 7c4915b83, 2026-09-23)

Re-ran on the merge-forward head `7c4915b83` (develop base `8bb344e32`):

- `uv run pytest test_capabilities_routes.py test_chat_voice_gates.py test_legacy_dag_node.py -x -q`: 77 passed.
- `uv run ruff check .`: green; `scripts/check-suite-inventory.py --suite packages/hive-conductor/backend/tests`: ok (2670).
- Mutation A and Mutation B re-executed independently; both targeted tests FAILED under the mutation and each file was restored byte-identical (`git status` clean).
- Commit/PR closure-keyword scan: no fixes/closes/resolves tokens ("Refs #1094" only).

## Repair-session validation (final tree, 2026-09-23)

Two focused changes on top of the verifier pass, both evidence-driven:

1. `test_delegated_authority_authorizes_only_through_its_signature` — the
   acceptance criterion names delegated authority explicitly, but only the
   human kind had a positive end-to-end test. The new test proves a signed
   `kind="delegated"` authority authorizes the exact workflow while the
   identical evidence with the signature stripped is refused, so the actor
   string alone never delegates.
2. `gate_tool_dispatch` refusal naming: a canonical decision that failed
   verification was audited as `approval_required` (a missing grant) instead
   of `invalid_approval_evidence` (a bad one), because the reason branched
   only on the legacy evidence dict. Now any presented-but-unverified
   evidence names the invalid reason, so the audit row distinguishes a
   missing grant from a forged one.

Final-tree evidence:

- `uv run --no-sync pytest tests/test_capabilities_routes.py tests/test_chat_voice_gates.py tests/test_legacy_dag_node.py -q`: 78 passed.
- `uv run --no-sync ruff check .`: green; `uv run --no-sync ruff format --check .`: 2528 files already formatted.
- `uv run --no-sync mypy packages/maistro-core/src`: only the five pre-existing `import-not-found` errors for the absent optional `maistro_bootstrap` extra; none in changed files.
- `scripts/check-suite-inventory.py --suite packages/hive-conductor/backend/tests`: ok.
- Mutations A and B re-executed together on the final tree; both targeted tests FAILED, both files restored byte-identical.
- `scripts/check-gates-ran.py` requires `--check-runs` (GitHub check-run JSON) and is an integration-time gate; its local exit 2 without that input is a usage constraint, not a code defect.
- Worktree note: plain `uv run pytest` fails in this worktree because hatchling cannot build the `hive-conductor` editable wheel (path-rewrite prefix limitation, pfmoore/editables#20); the pre-populated worktree `.venv` with `--no-sync` is the working invocation. Environment quirk, not a code defect.

## Repair-lane re-validation (head 7bbcb2e33, 2026-09-24)

The prior verify-lane evidence was rejected only because its own evidence
commit changed the worktree (`failure_kind: worktree_changed`); the code
head under review is unchanged. This job dir carries no driver check logs,
so every acceptance check below was re-executed here, not trusted:

- `uv run pytest test_chat_voice_gates.py test_capabilities_routes.py
  test_legacy_dag_node.py -q`: **78 passed**.
- All 10 named acceptance tests run individually: 10 passed
  (`test_scan_config_visits_nested_airtable_field_name_keys`,
  `test_airtable_field_name_in_nested_mapping_key_is_withheld`,
  `test_tool_result_gate_scans_exact_serialized_mapping_keys`,
  `test_run_workflow_is_privileged_and_requires_scoped_approval`,
  `test_delegated_authority_authorizes_only_through_its_signature`,
  `test_canonical_human_approval_is_exactly_scoped_and_audited`,
  `test_canonical_approval_reaches_only_the_intended_handler_call`,
  `test_workflow_forged_evidence_never_reaches_handler`,
  `test_legacy_mutation_is_refused_by_independent_effect_policy`,
  `test_standalone_canonical_node_cannot_bypass_effect_policy`).
- Prior-findings remediation tests: `test_actor_string_without_typed_authority_cannot_approve_workflow`,
  `test_workflow_approval_route_rejects_config_editor_as_approver`,
  `test_unavailable_workflow_approval_is_audited`, plus the three
  reachable-route tests — 6 passed.
- `uv run pytest packages/maistro-core/tests/capabilities/test_inbox_approval.py
  test_host_health_action.py -q`: 13 passed (provider surfaces unchanged).
- Mutations applied only via `/tmp` pytest plugins (tree untouched):
  - A (mapping-key yields dropped from `_text_leaves`):
    `test_scan_config_visits_nested_airtable_field_name_keys` **FAILED**.
  - B (`TOOL_EFFECTS["run_workflow"] = TOOL_EFFECT_NETWORK`):
    `test_run_workflow_is_privileged_and_requires_scoped_approval` and
    `test_workflow_forged_evidence_never_reaches_handler` **FAILED**.
- `uv run ruff check .` green; `uv run ruff format --check .` 2534 files
  formatted; `uv run mypy packages/maistro-core/src`: only the five
  pre-existing `maistro_bootstrap` import-not-found errors; `scripts/
  check-suite-inventory.py --suite packages/hive-conductor/backend/tests`:
  ok (2678). `scripts/check-gates-ran.py` remains an integration-time gate.
- Code reads: `chat_completion.py` scans `_serialize_tool_result(result)` —
  the same `json.dumps` string appended as `role="tool"` content on both the
  streaming and non-streaming loops; `chat_gate.py` classifies `run_workflow`
  as `TOOL_EFFECT_MUTATE` and accepts only an HMAC-verified authority bound
  to requester, workflow id, and argument digest; `routes/capabilities.py`
  mints the signature server-side from the authenticated session principal
  and enforces admin/`approvals.resolve` for workflow actions;
  `middleware/auth.py` maps `/v1/capabilities/approvals` to
  `approvals.resolve` and `/v1/chat/workflows` to `dags.write`. No new
  workflow lifecycle, Run authority, or chat-private approval store: the
  approval travels through the shared `approval` capability slot.

## Independent verifier re-execution (final head f235aa777, 2026-09-23)

Re-derived acceptance from the issue text on the exact review head `f235aa777`
(develop base `8bb344e32`). Driver checks re-executed personally, not trusted
from logs: `uv sync --locked --extra dev` ok; `uv run ruff check .` green;
`uv run ruff format --check .` 2528 files formatted; `.venv/bin/python -m pytest
test_capabilities_routes.py test_chat_voice_gates.py test_legacy_dag_node.py -q`
78 passed; `scripts/check-suite-inventory.py --suite
packages/hive-conductor/backend/tests` ok (2671). Additionally `pytest
packages/maistro-core/tests/capabilities/test_governed_invocation.py
test_inbox_approval.py test_durable_approval.py test_invocation_reconciliation.py
-q`: 41 passed (core modules changed by this PR).

Mutations re-executed on this head with sha256-verified byte-identical restore:
- Mutation A (key yield dropped from `_text_leaves`):
  `test_scan_config_visits_nested_airtable_field_name_keys` FAILED (1 failed as
  required; the two serialized-path airtable tests still pass by design — the
  tool_result boundary scans the exact `json.dumps` string, which already
  embeds keys, and is separately guarded by
  `test_tool_result_gate_scans_exact_serialized_mapping_keys`).
- Mutation B (`run_workflow` -> `TOOL_EFFECT_NETWORK`): 4 FAILED as required
  (privileged-classification, actor-string-without-authority,
  exactly-scoped-audit, forged-evidence-never-reaches-handler — the last is a
  behavioral failure: forged evidence would reach the handler under network
  classification).
- Post-restore: both files sha256-identical, `git status` clean, suite re-run
  78 passed, ruff green.

Closure-keyword scan re-run on PR #1454 body ("Refs #1094" only) and all 12
commit bodies `8bb344e32..f235aa777`: no fixes/closes/resolves tokens.

## Repair-lane re-validation (merge-forward head 7625e81a6, 2026-09-24)

Independent re-execution on the exact merge-forward head `7625e81a6` (develop
base `1dea30dfe`); no production code changed in this session, mutations
restored from a local backup copy with `cp` (never `git restore`):

- `uv run ruff check .`: all checks passed; `uv run ruff format --check .`:
  2529 files already formatted.
- `.venv/bin/python -m pytest test_capabilities_routes.py
  test_chat_voice_gates.py test_legacy_dag_node.py -q`: 78 passed (pre- and
  post-mutation).
- Core capability suite (`test_governed_invocation.py test_inbox_approval.py
  test_durable_approval.py test_invocation_reconciliation.py`): 41 passed.
- `mypy` on the four changed `maistro.capabilities` modules: no issues.
- `scripts/check-suite-inventory.py --suite
  packages/hive-conductor/backend/tests`: ok (2671).
- Mutation A (key yield dropped from `_text_leaves`):
  `test_scan_config_visits_nested_airtable_field_name_keys` FAILED; restore
  sha256-identical, `git status` clean.
- Mutation B (`run_workflow` -> `TOOL_EFFECT_NETWORK`): all three targeted
  tests FAILED (`...privileged_and_requires_scoped_approval`,
  `...actor_string_without_typed_authority_cannot_approve_workflow`,
  `...workflow_forged_evidence_never_reaches_handler`); restore
  sha256-identical, `git status` clean.
- `scripts/check-gates-ran.py` without `--check-runs`: exit 2 = required
  argument missing (integration-time gate), matching the prior finding; not a
  code defect.
- Prior-finding spot checks on this head: `routes/capabilities.py` ignores
  body-supplied actors for HTTP resolution, requires admin or
  `approvals.resolve` for `run_workflow`, and mints the HMAC authority
  server-side; `middleware/auth.py` maps `/v1/capabilities/approvals` to
  `approvals.resolve` and `/v1/chat/workflows` to `dags.write`; the
  approval-capability-unavailable branch audits
  `chat_workflow_approval_refused` with `refusal_reason` before returning
  (`test_unavailable_workflow_approval_is_audited`).

Production-reachability checks by reading: only two `"role": "tool"` appends
exist (chat_completion.py:2267, :2624), both fed by `_gated_execute_tool` and
serialized via the same `_serialize_tool_result` the gate scanned; direct
`_execute_tool` sub-calls inside handlers (poll_jira/check_blockers/
search_jira fallback) only reach the model embedded in the outer gated result.
`/v1/chat/workflows/run` is registered (`main.py:344` prefix `/v1/chat`) and
covered by the `/v1/chat/workflows` protected-op prefix (`middleware/auth.py`
boundary-aware startswith), requiring `dags.write`; approval resolution
requires `approvals.resolve` (admin or elevated) with the HMAC authority minted
server-side only. No new workflow lifecycle, Run authority, or chat-private
approval store: shared capability approval inbox, existing
dag_run_store/execute_dag/authorize_hive_dag_scope, canonical Invocation
boundary reused.

## Independent verify-lane re-validation @ fdbbd9f49 (2026-09-24)

Head under review: `fdbbd9f49` (merge of develop `1dea30dfe` into the repaired
branch; `git diff --name-only 437c6b849..fdbbd9f49` touches none of the #1094
surfaces — merge-forward, fix set byte-identical). Re-executed on this head:

- `uv run pytest test_capabilities_routes.py test_chat_voice_gates.py
  test_legacy_dag_node.py -x -q`: 78 passed.
- `uv run ruff check .`: passed. Driver gates (uv sync --locked --extra dev,
  ruff format --check, suite inventory 2678): all green (job
  70f59e2f check-0..4 logs).
- Mutation A (mapping-key yield dropped from `_text_leaves`, applied via a
  /tmp pytest plugin — tree untouched):
  `test_scan_config_visits_nested_airtable_field_name_keys` FAILED as
  required; `test_airtable_field_name_in_nested_mapping_key_is_withheld`
  still passes because the chat boundary scans the serialized string, where
  keys are inherently present (defense in depth, not a gap).
- Mutation B (`TOOL_EFFECTS["run_workflow"] = TOOL_EFFECT_NETWORK`, same
  technique): `test_run_workflow_is_privileged_and_requires_scoped_approval`
  and `test_workflow_forged_evidence_never_reaches_handler` FAILED as
  required.
- Named acceptance tests re-run individually (8/8 passed): delegated-human
  signature gating, exact-scope audit, actor-string refusal, config-editor
  approver 403, unavailable-capability audit, verified-principal route,
  legacy mutation refused by independent effect policy, standalone canonical
  node cannot bypass effect policy.
- PR #1454 body + branch commits scanned for closure keywords
  (fixes/closes/resolves #...): none; PR is a draft claim-stake.

## Independent verify-lane re-validation @ 282f644be (2026-09-24, job dc19b979)

Head under review: `282f644be` (exact assigned head; merge of develop
`60862b6c5` into auto-1094; worktree clean at start). Re-executed:

- `uv run pytest packages/hive-conductor/backend/tests/test_capabilities_routes.py
  packages/hive-conductor/backend/tests/test_chat_voice_gates.py
  packages/hive-conductor/backend/tests/test_legacy_dag_node.py -q`: 78 passed.
- `uv run ruff check .`: passed. `scripts/check-suite-inventory.py`: ok (13
  suites; driver log recorded 2685 tests).
- Mutation A (drop mapping-key yield in `_text_leaves`, /tmp pytest plugin):
  `test_scan_config_visits_nested_airtable_field_name_keys` FAILED as required.
- Mutation B (`run_workflow` -> NETWORK, same technique):
  `test_run_workflow_is_privileged_and_requires_scoped_approval`,
  `test_canonical_human_approval_is_exactly_scoped_and_audited`, and
  `test_workflow_forged_evidence_never_reaches_handler` FAILED as required;
  the privileged test failed with `refused is not None` assertion — dispatch
  allowed without canonical approval, exactly the pre-#1094 defect.
- Mutation C (NEW this pass — keys dropped from the scanned serialized
  representation: gate scans a values-only re-serialization while the full
  `json.dumps` string is re-fed): `test_airtable_field_name_in_nested_mapping_
  key_is_withheld` FAILED as required. This closes the earlier note that the
  withheld test survives mutation A: a scanned representation that drops keys
  is caught by the adversarial test itself.
- Live PR #1454 rollup at this head (read-only `gh pr view ... statusCheckRollup`):
  `exact-debt-ledger`, `Quality gate (Pillars)`, `integration-scope` (aggregator),
  `coverage (MinIO)`, `object storage (MinIO)`, and `gates-ran` FAILING;
  `test` and `docker-build` IN_PROGRESS; lint/type, SAST, postgres x2, e2e x2,
  coverage (no-services/PostgreSQL), formal-conformance, Gate C green.
- Vulture ratchet reproduced locally: `scripts/check-vulture-baseline.py` exit 1
  at this head — 206 fastapi-route-handler identities unbanked. Same scan on an
  extracted base tree (`git archive 60862b6c5`): identical 1432 findings,
  identical flagged-path set, and identity-set diff base->head is EMPTY
  (`comm -13` = 0): this branch introduces zero new vulture debt; the failure is
  inherited from the develop WIP base and is not caused by the #1094 fix set.
  Base commit CI also shows `Gates Ran` failures. PR is therefore not
  integration-green at this head, but the redness predates it.
- Closure keywords re-checked on PR body and all branch commit messages
  (fixes/closes/resolves #…): none; PR remains a draft claim-stake.
