---
inventory-delta:
  tests/: -5
---
# auto-1096-8c60

Recorded count for `tests/` moves 3513 -> 3508 with the develop merge
(ff6fdb16-era tip ffd6fdb16 into auto-1096). All of it is explained, nothing
was silently lost:

- **The recorded 3513 was already stale on develop itself.** Collecting
  `tests/` at MERGE_HEAD (ffd6fdb16, verified in a detached temporary
  worktree) yields **3506**, not 3513 — develop's own tree no longer matches
  its recorded number by +7.
- **#1096 adds 4 tests**: `tests/test_check_security_inventory.py` grows 39 ->
  43 (sibling census, alias-following, repo constructor census, and the
  function-local `httpx` import regression test).
- **#1096 removes 2 parametrized cases**:
  `tests/test_http_client_sites.py` goes 6 -> 4 because
  `test_every_allowlist_entry_still_constructs_a_client` is parametrized over
  `ALLOWED`, and this branch emptied the two obsolete exemptions
  (`maistro-evolve .../openai_compatible.py`, `maistro-design
  .../open_design.py`) by routing both through the guarded seam.

3506 + 4 - 2 = 3508. Per-file collection counts were diffed between MERGE_HEAD
and the merged tree; only the two files above differ.
