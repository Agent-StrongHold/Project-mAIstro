---
inventory-delta:
  packages/maistro-core/tests: +10
---
# auto-1097-c7a5

The durable HITL deadline regression coverage now exercises the missing,
blank, and non-string verdict cases across all three verdict nodes. It also
proves repeated malformed submissions remain PAUSED through SQLite restarts,
expire at the original durable deadline, allow a later valid answer to settle
before expiry, and prove a malformed-answer/timeout race cannot revive the
terminal Run. Node-level coverage also proves an invalid carried pause marker
cannot fall back to a fresh wall-clock deadline.
