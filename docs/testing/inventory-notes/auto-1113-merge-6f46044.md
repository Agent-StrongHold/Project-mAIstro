# auto-1113 merge reconciliation: #1079 resolver kwargs meet the no-spine refusal

Second `develop` merge into `auto-1113` (#1113 retirement of the Hive
standalone pre-convergence Graph execution fallback), merging develop through
`6f4604420` (DagBuilder Run-button truthfulness, #1551). One textual conflict,
in `backend/services/dag_agents.py`, resolved by keeping both sides' intent:

- **Kept (ours, #1113):** `_canonical_execution_stores()` raises
  `GraphExecutionUnavailableError` before any admission when the Container or
  either canonical store is missing; `_resolve_nodes_with()` has no
  `_fallback_node_resolver` path.
- **Took (develop, #1079):** `build_node_resolver(...)` now forwards
  `effect_context=container.capability_effects`,
  `provider_registry=container.provider_registry`, and
  `llm_router=container.llm_router`, read as direct attribute loads so
  `check-wiring-reads.py` (#236) can hold the wiring in place.

The no-spine refusal is untouched by the new kwargs: they are only read after
`_canonical_execution_stores()` has succeeded.

No collected-test count moved (fixture-only updates, hence no
`inventory-delta` block):

- `test_dag_agents.py::_canonical_test_container` and
  `test_node_metrics_are_measured.py::canonical_container` — both stub
  containers now model the real #1079 Container contract
  (`capability_effects`/`provider_registry`/`llm_router`) that the repaired
  resolver reads; without them the merged suite failed with
  `AttributeError: ... no attribute 'capability_effects'`.

Validated: full `packages/hive-conductor/backend/tests` (2576 passed,
1 skipped), `packages/maistro-core/tests/graph/durable_runs` (430 passed),
`maistro-server` runs/chat API suites, ruff check + format,
`check-wiring-reads`, `check-convergence-matrix`, `check-execution-lifecycles`,
`check-shipped-surface-truth`, `check-m1-convergence-freeze --base 5f7088dd`,
`check-owned-store-access`, `check-merge-markers`, `check-suite-inventory`.
