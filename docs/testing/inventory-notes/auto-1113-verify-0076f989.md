inventory-delta:
  packages/hive-conductor/backend/tests: +0
  packages/maistro-core/tests: +0
---
# auto-1113 independent verification round at head 0076f9895d46

Verify-only round (job 26117e2547e547bd8089e0c44f67eeb6) at exact head
`0076f9895d46` (branch `auto-1113`, base `b906cc577fbb`). Worktree clean; the
prior develop-sync block is already resolved by merge commit `c89c011e6`
(plus delta commits `5eb048b01`, `0076f989`) — no conflict markers remain and
no further merge was performed (verifier read-only rule). No code or test
files were touched this round.

## Re-executed evidence (all observed passing at this head)

- `uv run pytest packages/hive-conductor/backend/tests -q` → 2772 passed,
  6 skipped. Skips are environment-gated only (1 CSP frontend-not-built,
  5 `MAISTRO_TEST_PG_DSN` workspace-authority); none hide #1113 evidence.
- `uv run pytest packages/maistro-core/tests/graph/durable_runs
  packages/hive-conductor/backend/tests/test_dag_recovery.py -q` →
  562 passed, 39 skipped.
- `uv run ruff check .` → all checks passed.
- `uv run python scripts/check-convergence-matrix.py` → OK (52 subsystems /
  1125 modules, 188 attributed unreachable).
- `uv run python scripts/check-execution-lifecycles.py` → OK (19 lifecycles,
  ratchet base b906cc577 → candidate 0076f989).
- Named tests re-run individually, all PASSED:
  `test_dag_agents.py::test_registered_dag_fails_closed_without_the_canonical_spine`,
  all 4 `test_graph_spine_architecture_gate.py` tests,
  `test_optimizer.py` 3 unavailable/spine tests,
  `test_substrate_tools.py::test_tool_run_workflow_reports_unavailable_when_the_run_is_not_completed`.

## Acceptance re-derivation

1. `run_registered_dag` fails closed: `services/dag_agents.py`
   `_canonical_execution_stores()` raises `GraphExecutionUnavailableError`
   before `create_run` when the Container or either canonical store is
   absent; no `_fallback_run_store` remains.
2. `execute_dag` cannot manufacture compat scope: `canonical_dag_runner._scope`
   requires a `DagExecutionScope`, rejects workspace/project mismatch, and
   demands both canonical stores; `hive-standalone-compat` has zero matches in
   shipped code.
3. Bridge absence ⇒ explicit `status:"unavailable"` result (optimizer route
   returns the whole result unavailable with `proposals=[]`,
   `validated=false`; `validation_gate` and `substrate_tools` project the
   same), never a silent in-memory success.
4. `InMemoryDurableRunStore` has zero constructions outside `tests/`;
   the architecture gate AST-walks the shipped Hive backend and fails on
   `run_durable_graph` without/with-`None` `run_store=` or any forbidden
   fallback name. All 7 shipped call sites pass a canonical `run_store`.
5. Legacy archive: readable proven against the committed byte-for-byte
   fixture (14 tests); resumption refused by name (`LegacyRunNotResumable`)
   per Accepted ADR-082826-d9f5 AC-6 — the documented compatibility contract,
   and the refusal is what keeps new pre-convergence Runs impossible.
6. Restart/replica: `test_dag_recovery.py::test_admitted_run_persists_
   execution_mode_needed_after_restart` plus AC-5 continuation restart tests
   in the 562-test run; execution authority is Container/DB-backed only.
7. EngineService truth: `graph_execution_available` / `run_store` /
   `schedule_admitter` are None-with-docstring; StubAgentPort mode reports
   degraded capability honestly (`test_engine_service.py`,
   `test_agent_port_truthful_unavailable.py` in the 2772).
8. Census truth: CONVERGENCE-MATRIX recurrence row and boundary bullet state
   the fail-closed post-#1113 behavior; matrix + lifecycle gates OK.

## Closure hygiene

No `fixes/closes/resolves` keywords in any commit message
(`b906cc577..0076f989`) and the PR #1309 body uses "Refs #1113" only.

Verdict: MERGE-READY (writer handoff; integration approval remains with CI).
