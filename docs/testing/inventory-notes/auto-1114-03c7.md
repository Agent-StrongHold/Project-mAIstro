---
inventory-delta:
  packages/maistro-core/tests: +11
---
# auto-1114-03c7

Eleven net new collected nodes for this branch's #1114 repair, all in
`tests/tasks/test_admission.py`.

The original +1 was `test_recovered_task_runs_under_the_original_canonical_identity`:
a recovered task executes under its original Run/NodeRun/Attempt chain rather
than a replacement Run.

The repair pass (CI head 9bdc4c747: diff-coverage below the 90%/80% per-file
floor on `tasks/queue.py`) added eight recovery-branch tests:

- malformed-payload variants beyond the missing-payload case — a payload whose
  `task_id` disagrees with Run provenance, and a payload whose status is no
  longer `queued` — both terminalized visibly on the canonical Run;
- recovery of a store with no QUEUED runs is a no-op, and a non-positive
  `batch_size` is refused as a caller bug;
- QUEUED Runs from other admission sources (`schedule`) are left to their own
  consumer (#251), never stolen into a second lifecycle;
- a receipt still held in memory is not re-enqueued by a racing `recover()`;
- recovery yields a Run a concurrent worker already claimed (the canonical
  QUEUED→RUNNING transition is the fence) and survives a store that refuses
  the terminal FAILED write.

Two more run against real PostgreSQL (`MAISTRO_TEST_PG_DSN`), closing the
restart-test mandate at both crash boundaries on the durable store itself:
a death after admission rehydrates and executes under the original
Run/NodeRun/Attempt chain, and a Run whose durable payload was corrupted is
terminalized FAILED instead of staying QUEUED forever.

The earlier reconciliation story (`+10` → `+8` after the develop merge) is
superseded by this note's own delta accounting: 7 tests in
`1114-task-run-recovery.md`, plus these 11 here.
