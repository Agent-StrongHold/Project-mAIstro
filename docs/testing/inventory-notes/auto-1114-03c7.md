---
inventory-delta:
  packages/maistro-core/tests: +1
---
# auto-1114-03c7

One net new collected node for this repair branch: the recovered task executes
under the original Run/NodeRun/Attempt identity
(`test_recovered_task_runs_under_the_original_canonical_identity`).

The other #1114 coverage — durable payload on the QUEUED Run, both crash
boundaries (after Run admission, and after receipt creation before enqueue
notification), duplicate-receipt canonical fencing, malformed-payload
terminalization, and PostgreSQL rehydration — is recorded by
`1114-task-run-recovery.md` (+7).

This note previously said `+10`, double-counting those seven tests against a
pre-merge base. Re-running `--update` while resolving the develop merge
(base 0221d2cd -> ba2f1f077) reconciled the ledger to the branch's actual
addition: 8 collected nodes in `tests/tasks/test_admission.py`, 7 recorded
there plus 1 here.
