---
inventory-delta:
  packages/hive-conductor/backend/tests: +25
  tests/: +9
---
# auto-1134 — CI repair: cover the namespace-migrated lazy imports

Repair for the CI failures at 17a5e47e45836aa83d448c9fa88fd72a8a834a59. The
#1134 namespace migration rewrote lazy imports (`from services.x import y` →
`from hive_conductor.services.x import y`) inside route handlers, tool
handlers and gate scripts. Those rewritten lines became "changed lines" for
the diff-coverage gate, and several suites never executed them, so the
Coverage gate failed per-file. The devskim check also failed (DS126858,
error severity) on `scripts/check-suite-inventory.py`'s sha1 note-slug
digest once this branch's edits shifted its line number; the digest is now
blake2b.

- `packages/hive-conductor/backend/tests` +25: degraded-path route tests for
  evolution/rsi/missions/setup-checklist/providers/projects
  (`test_migrated_route_endpoints.py`), service-level lazy-import tests for
  chat_completion tool handlers, the optional PG memory wrappers,
  pipeline_orchestrator's trigger, ui_auto_climb and credential_store_v2
  (`test_migrated_service_imports.py`), one memory-decay no-arg start test,
  and six substrate DAG-tool handler tests (which also fixed
  `tool_list_workflows` iterating a JsonStore via the sequence protocol —
  it crashed with KeyError on the real store).
- `tests/` +9: check-backend-package-namespaces gains the missing-`__init__`,
  stray-Python-in-package, `main()` failure and real `__main__` entry-point
  tests; check_enumerations gains a test that runs `check_routes()` against
  the real app and asserts every found gap is baselined.
