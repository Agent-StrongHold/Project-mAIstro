---
inventory-delta:
  packages/maistro-core/tests: +4
---
# Issue 232 task attempt recovery

Adds the task crash-recovery conformance coverage to the canonical spine suite. The test drives a leased task Attempt to durable RUNNING state, stops renewal, reclaims and reconciles it through the canonical lifecycle, then verifies retry creates ordinal 2 while preserving the cancelled ordinal 1. The parameterized PostgreSQL leg exercises the persisted backend when the database fixture is available; memory and SQLite provide local coverage. SQLite also proves a sweep cannot overwrite a renewal made through another database connection.
