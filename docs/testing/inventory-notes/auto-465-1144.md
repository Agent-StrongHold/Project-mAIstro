---
inventory-delta:
  tests/: +3
---
# auto-465-1144

The shipped-surface discovery regressions add four collected test cases for
Starlette's positional `add_route` methods registration and for an inert
`object()` local placeholder that must not hide a fake-success response. The
recorded net delta is +3 because the starting head collected one fewer root
suite node than the previously recorded expected count.
