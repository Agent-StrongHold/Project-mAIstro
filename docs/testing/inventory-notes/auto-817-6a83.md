---
inventory-delta:
  packages/maistro-core/tests: +3
  packages/maistro-design/tests: +6
---
# auto-817-6a83

Round-20 repair for #817: renderer-parity for unknown/inert-list-excluded tags
(AC-2/AC-4). Nothing was removed; two stale fixtures were retargeted to
boundary-clean fragment markup.

## Finding — the renderer blocks every non-allowlisted tag; the pre-scan didn't

`scrubTree()` in `visualArtifactRenderer.tsx` removes every element outside
`HTML_TAGS`/`SVG_TAGS` and classifies it `active-element`, but the shared
scanner's `VISUAL_ARTIFACT_PATTERNS` enumerated only a fixed dangerous-tag
list. Executed probe: `scan_and_record("<marquee>hello</marquee>")` returned
T3 / empty flags / `upgrade` while the browser boundary blocked the same
markup — the exact contradiction #817 exists to prevent.

The shared vocabulary now classifies unknown tags: `VISUAL_ARTIFACT_INERT_TAGS`
(warden/patterns.py, pinned to the TS sets by test) plus
`visual_artifact_unknown_tag_names()`, wired into the `visual_artifact=True`
branch of `scan_blocking_patterns` over raw content and an invisibles-stripped
NFKD view — never `normalize_for_detection()`, whose leetspeak folding rewrites
real tag names (`h1` → `hi`) no browser ever folds.

## New tests

- `packages/maistro-core/tests/security/warden/test_visual_artifact_vocabulary.py`
  (+3): allowlist lockstep against the TS source, the exact `<marquee>` /
  `<custom-widget>` / `<blink>` regression through scanner AND
  `scan_and_record`, and an over-broad-catch-all guard (inert markup, prose
  `3 < 5`, doctype/comments stay clean).
- `packages/maistro-design/tests` (+6): `<marquee>` and `<custom-widget>`
  added to the trust pre-scan `HOSTILE_CORPUS` (parametrized over the SKULL,
  shared-verdict-parity, and recommendation tests); `<html></html>` and
  `<marquee>` added to the output-boundary hostile parametrize in
  `test_design.py`.

## Fixture retargets (no coverage lost)

- `test_design.py::test_multi_format_content_produces_container_root` and
  `test_persist_blobs_calls_store_blob_for_each_blob_leaf`: fixture HTML
  `<html></html>` → `<section>ok</section>`; the old fixture was itself
  renderer-blocked (active-element) — `<html></html>` moved into the hostile
  parametrize list as a regression.
- `test_design_renderers.py::test_render_to_png_raises_501…` and the bundled
  `workspace` system's `DESIGN.md` (`as <role>` → `as [role]`): the
  documentation placeholder parsed as an unknown tag, so generated decks
  failed the now-correct output scan; `[role]` cannot parse as an element in
  any medium.
- `deck-sanitization.spec.ts`: sanitize payloads gained `<marquee>` and
  `<html>` cases, scan/recommend arrays gained unknown-tag assertions
  (browser side of the same parity proof; runs in the Playwright CI job).
