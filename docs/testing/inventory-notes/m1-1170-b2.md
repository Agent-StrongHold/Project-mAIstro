---
inventory-delta:
  packages/maistro-core/tests: +13
---
# m1-1170-b2

Thirteen new tests in `tests/runs/test_chat_attempt_recovery.py` (#1170), all
additions, no removals, no renames:

- the lease contract (6): chat wired with the canonical lease through
  production `route_request`, the default TTL equal to the schedule consumer's
  `DEFAULT_SCHEDULE_LEASE_TTL`, the lease issued at the configured TTL,
  `lease_ttl=None` opting out, a non-positive TTL refused at construction, and
  a slow turn surviving its TTL via the heartbeat
- crash, reclaim, and the fence (4): a crashed chat worker's expired lease is
  reclaimed by the canonical `recover_abandoned_attempts` tick, repeated
  recovery is idempotent, a late stale completion cannot rewrite a reclaimed
  Attempt, and the dead worker's fencing token cannot touch the retry
- terminal-write failure is not swallowed (3): `_close_chat_run` reports the
  failed terminal write to its caller instead of hiding it, a closure failure
  never replaces the turn's own exception, and a completed Attempt stranded
  under a RUNNING Run by failed settle writes is re-derived by the canonical
  reconciliation authority

Verified +13 = collected (9564) vs recorded (9551); every suite besides
`packages/maistro-core/tests` unchanged.
