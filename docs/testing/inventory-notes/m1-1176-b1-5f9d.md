---
inventory-delta:
  packages/maistro-server/tests: +1
  tests/: +1
---
# m1-1176-b1-5f9d

Repair round 4 (this worktree) — closing the executed-probe findings from the
verify pass on `0e4d62af`. Two tests, each pinning one production fix:

- **`tests/` +1** — `tests/migrations/test_migration_chain.py`:
  `test_adopts_runtime_provisioned_table_without_a_primary_key`. The live
  probe showed a runtime-provisioned `task_idempotency` table that carried
  all ten columns but **no primary key** was adopted as-is by migration 033:
  `upgrade head` stamped the revision and the store's first
  `INSERT ... ON CONFLICT (scope_key)` then failed — no unique constraint
  existed to conflict on. The migration's reconcile path now reconstructs
  the primary key (a duplicate `scope_key` fails the `CREATE` loudly, which
  is the honest outcome), and this test drives that exact shape — built by
  hand at `032`, upgraded, then asserted on the live catalog — through the
  real-server-gated chain suite. The downgrade also became idempotent
  (`DROP ... IF EXISTS`), matching every earlier migration in the chain.
- **`packages/maistro-server/tests` +1** —
  `tests/api/test_tasks_idempotency.py`:
  `test_the_idempotency_key_header_is_allowed_cross_origin`. The wire-level
  suite mounts only the router, so it could not see that the production
  `CORSMiddleware` allow-list omitted `Idempotency-Key` (and the two
  workspace-scope headers) — a cross-origin preflight carrying the retry
  identity was rejected with `400 Disallowed CORS headers`. This test
  inspects the production app's own middleware configuration, the same seam
  `test_the_run_id_header_is_readable_cross_origin` guards for the Run-Id
  header. Executed against the default allowed origin: all three preflights
  now answer `200` and echo the headers.
