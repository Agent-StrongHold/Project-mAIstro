---
inventory-delta:
  packages/maistro-core/tests: +49
  packages/maistro-server/tests: +7
---
# m1-1176-b1

Task admission idempotency (#1176): a stable admission key — supplied or
payload-derived, scoped to the authenticated principal and the effective
Workspace — so a retry reconciles to the original admission instead of minting
a second canonical Run. All additions, no removals.

**+49 `packages/maistro-core/tests`**, in two files:

- `tests/tasks/test_idempotency.py` (+31): the scope digest's no-collision /
  no-leak properties across principals, Workspaces and actions (including the
  length-prefix aliasing case); the fingerprint's coverage of the payload and
  its exclusion of the auth-owned `user_id` and the key slot; key
  normalization (conflicting sources, oversized, whitespace-absent); the
  in-memory claim store's window, mismatch, takeover, release and purge
  semantics; and the queue integration — explicit-key retry, derived-key
  retry, mismatch refusal, effective-Workspace scoping through the routing
  admitter, concurrent identical submissions minting one Run, failed admission
  releasing its claim, replay after the receipt left memory, provenance
  carrying the caller's key, and the unwired-store parity path.
- `tests/tasks/test_idempotency_durable.py` (+18): the SQLite tier against a
  real file (restart survival, two-connection claim refusal, durable
  mismatch/window/takeover, purge, schema idempotence, the takeover guard
  agreeing with the read-side classification), and the PostgreSQL tier at the
  fake-asyncpg boundary (`test_pg_strikes.py` pattern) asserting the emitted
  statements — the `ON CONFLICT (scope_key) DO NOTHING` claim, the takeover
  guard, the `task_id IS NULL` pending guards, the bounded purge — plus the
  backend-selection probe in `wire_task_idempotency`.

**+7 `packages/maistro-server/tests`** — `tests/api/test_tasks_idempotency.py`:
the HTTP wire format on `/tasks`. Retry with the standard `Idempotency-Key`
header reconciles to the original task_id/run_id; a reused key with a
different payload is a 409 carrying no run; header/body key conflict is a 422
and agreement is accepted; byte-identical bodies without any key reconcile by
derivation; two authenticated callers sharing one textual key get their own
Runs and cannot read each other's receipts; and the no-key parity case is
unchanged from before.

Verified +49/+7 = collected (9597 / 355) vs recorded (9548 / 348); every suite
besides those two unchanged.
