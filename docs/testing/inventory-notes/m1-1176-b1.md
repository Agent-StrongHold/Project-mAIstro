---
inventory-delta:
  packages/maistro-core/tests: +16
  packages/maistro-server/tests: +6
  tests/: +4
---
# m1-1176-b1

Task admission idempotency (#1176): a stable admission key — supplied or
payload-derived, scoped to the authenticated principal and the effective
Workspace — so a retry reconciles to the original admission instead of minting
a second canonical Run. All additions, no removals.

**+70 `packages/maistro-core/tests`**, in two files:

- `tests/tasks/test_idempotency.py` (+46): the scope digest's no-collision /
  no-leak properties across principals, Workspaces and actions (including the
  length-prefix aliasing case); the fingerprint's coverage of the payload and
  its exclusion of the auth-owned `user_id` and the key slot; key
  normalization (conflicting sources, oversized, whitespace-absent); the
  in-memory claim store's window, mismatch, takeover, release and purge
  semantics; the claimant-token fence — a superseded claimant's complete and
  release land nowhere; `begin` announcing the receipt id, refusing a
  superseded or repeated announcement, and refreshing the lease; the begun
  claim past its lease classifying as ambiguous rather than takeover;
  discovery resolving the ambiguity to the minted Run via `resolve_run`, or
  freeing it for the `task_id`-fenced `take_over_resolved`; release after a
  begun failure; the shared `_ClaimFlow` loop under interleaving — a slot
  freed between the takeover guard's read and its write is re-won, a
  freed-then-restolen slot reports the winner's pending claim, and takeover
  rounds are bounded with the standing row reported; the in-memory bound's
  eviction order; and the queue integration — explicit-key retry, derived-key
  retry, mismatch refusal, effective-Workspace scoping through the routing
  admitter, concurrent identical submissions minting one Run, failed admission
  releasing its claim, replay after the receipt left memory, a header-only key
  surviving the restart replay, provenance carrying the caller's key, the
  unwired-store parity path, the bounded pending timeout, and the ambiguous
  window end to end — a takeover mid-mint leaves exactly one standing Run with
  the superseded mint cancelled, a death after the mint resolves to the
  existing Run by discovery, and a death before the mint is taken over without
  a duplicate.
- `tests/tasks/test_idempotency_durable.py` (+27): the SQLite tier against a
  real file (restart survival, two-connection claim refusal with the stale
  token refused across connections, the begin/ambiguous/resolve/take-over
  protocol on real rows, durable mismatch/window/takeover, purge, schema
  idempotence, the takeover guard agreeing with the read-side classification);
  the PostgreSQL tier at the fake-asyncpg boundary (`test_pg_strikes.py`
  pattern) asserting the emitted statements — the `ON CONFLICT (scope_key) DO
  NOTHING` claim carrying the claim token, the takeover guard, the
  token-fenced begin/complete/release guards, `resolve_run` and the
  `task_id`-fenced `take_over_resolved`, the bounded purge, the provision DDL
  mirroring migration 034's column set — plus the wiring: a spine-ready pool
  is provisioned at wire time (never dropped to in-memory state), an
  unprovisionable spine-ready pool FAILS the wiring, an unmigrated or dead
  pool never receives claims at all (the claims tier follows the spine's own
  `spine_is_migrated` probe down); and the whole contract re-proven against a
  real PostgreSQL server (skips without `MAISTRO_TEST_PG_DSN`): claim, death
  mid-mint, ambiguous resolution, stale-token refusal, the resolved takeover
  across pools, and the wiring decision agreeing with the spine probe on the
  live server whichever way its migration state answers.

**+11 `packages/maistro-server/tests`** — `tests/api/test_tasks_idempotency.py`:
the HTTP wire format on `/tasks`. Retry with the standard `Idempotency-Key`
header reconciles to the original task_id/run_id; a reused key with a
different payload is a 409 carrying no run; header/body key conflict is a 422
and agreement is accepted; byte-identical bodies without any key reconcile by
derivation; two authenticated callers sharing one textual key get their own
Runs and cannot read each other's receipts; and the no-key parity case is
unchanged from before. The durable half puts the same wire on the SQLite
claim tier via the ASGI transport: a timeout retry reconciles across a
restart (receipt reconstructed, header key echoed, one Run), a derived key
reconciles across a restart, five concurrent in-flight POSTs mint exactly one
Run, and a replica handoff — a second connection to the same claim file —
reconciles the first replica's claim.

Measured at this head: 9657 core / 359 server collected, every suite green
except the pre-existing environment-gated skips (sandbox needs bwrap user
namespaces this WSL runner does not grant; real-PG needs `MAISTRO_TEST_PG_DSN`).
Delta vs the recorded pre-repair head: +3 core (the wiring tests), server
unchanged.

## Repair round 2 (this worktree)

Audit finding `idempotency.py:1236-1248` — *returns `InMemoryTaskIdempotencyStore`
after configured PostgreSQL provisioning failure, violating the
no-process-local-cache stop condition* — was the one live item: the earlier
repair commit (`a2feb6c1`) had already closed the claim-fence and
ambiguous-window findings (`queue.py` begin-before-mint + token fences +
compensation; the death-after-mint / death-before-mint tests), the durable
server E2E had replaced the in-memory-runstore restart simulation with real
SQLite claim files and fresh connections, and the PR-head mismatch is a
GitHub-plumbing observation this worktree cannot act on (no GitHub mutations).

Repairing the finding exposed that the real defect was **tier selection**, in
both directions. `wire_task_idempotency` landed claims on any configured
`pg_pool`, while `wire_execution_spine` lands Runs there only when
`spine_is_migrated` passes — so the container's own comment ("Same backend the
spine just chose") was false whenever a pool was configured but unmigrated:
durable claims beside an in-memory spine make a restart-retry *replay a
receipt whose Run died* — a false reconciliation, work silently lost. The
opposite corner was the audited one: provisioning failure degraded claims to
process-local beside a spine that might be durable, minting a second Run for
the first retried submission after a restart. The repair makes the container's
promise true by construction:

- `runs/wiring.py`: the probe goes public as `spine_is_migrated` (one
  definition — the tiers cannot drift on which pool is durable).
- `tasks/idempotency.py`: claims land on the pool only when the spine's probe
  says the spine lives there; provisioning failure on a spine-ready pool now
  raises `ConfigError` and the process does not start (fail-closed — no
  process-local fallback on a configured healthy PostgreSQL); a pool the spine
  refused gets no claims at all, which follow the spine down to SQLite or
  in-memory, loudly. The deferred import is forced: `runs.wiring` imports
  `tasks.admission`, which imports this module.

**Reconciliation note (issue text vs convention).** The spine's accepted
convention (`spine_is_migrated`, with #122/#132 provenance in its docstring)
is loud-degrade, never startup-crash, for a configured pool the spine itself
cannot make durable — and claims-in-memory beside an ephemeral spine is the
honest tier there, not a cache in front of durability. The one case this
repair still refuses to start for is a spine-ready pool whose claims DDL is
refused: durability asymmetry against a *healthy* database is a
misconfiguration, and the wire says so.

## Repair round 3 (this worktree) — executed-probe evidence closed

The verify pass on `d1f31c78` ran live probes against the runtime, and two of
its three code findings were real defects in production behavior (the third
was a GitHub-plumbing observation this worktree cannot act on). Both are
repaired here, each reproduced first and re-run to green:

**Migration 034 vs runtime self-provisioning.** Reproduced on a temporary
PostgreSQL: database at revision 032, `PgTaskIdempotencyStore.ensure_schema()`
provisions the claims table at wire time (the documented behavior — wiring
does not wait for the chain), then `alembic upgrade head` exited 1 with
`DuplicateTable: relation "task_idempotency" already exists`. A chain that
cannot reach head on exactly the deployments that provisioned early is broken
at the command every documented setup path runs. The upgrade now inspects what
is standing: all ten columns present → adopt the runtime's table (create the
purge index if the provisioning half-failed, stamp the revision); columns
missing → a loud refusal naming the missing columns, never a stamp over a
foreign shape. Columns are spelled once (`CLAIM_COLUMNS`) so the create and
the check cannot drift. Tests: `tests/migrations/test_migration_chain.py`
gains `TestTheChainSurvivesRuntimeSelfProvisioning` (+2, real-server gated
like the rest of that file) — the provision-then-upgrade sequence driven
through the real `ensure_schema`, and the refusal edge, which also cleans up
its foreign table because the `empty_database` fixture can only downgrade what
the chain owns. Executed: full migration file 13 passed against a live
PostgreSQL, and the round-trip/refusal paths verified by hand.

**The crash-after-create window stranded the Run it reconciled to.**
Reproduced at the queue seam: a claimant dying after minting its Run but
before the receipt reached any queue left the claim resolving — by the
contract's own terms — to a Run stuck at `created`, a replayed receipt saying
`queued`, and a restarted queue holding nothing: reconciliation answered the
retry while silently stranding the work. The replay path now finishes the
crash victim's own last step: `record_transition(run_id, QUEUED)` is the gate
(true exactly when the Run is CREATED or QUEUED — it advances a CREATED Run
and refuses RUNNING/terminal work that re-enqueuing would duplicate), and the
reconstructed receipt re-materializes into the queue the reconciling retry
landed on. Two racing replicas re-enqueueing the same QUEUED Run is still
safe: the Run spine refuses the second QUEUED→RUNNING, so the loser's task
fails visibly rather than executing twice. No spine → no Run state to consult
→ receipt replays unqueued (the no-database tier's documented ephemerality).
Tests: core `test_idempotency.py` gains the crash-between-mint-and-queue
resumption test and the RUNNING refusal-edge test (+2), and the
restart-replay test now asserts the reconciled work is executable rather than
asserting the old stranding (`receipts == []`). Server E2E gains the test the
contract actually describes (+1): a genuine client-side timeout
(`asyncio.wait_for` around the POST) that cancels the request mid-admission
*hanging at the outcome record* — the ambiguous crash-after-create window —
over the fully durable tier (SQLite claim file AND SQLite Run spine, each
"process" opening its own connections), then a restart, a lapsed lease, and a
retry asserted to reconcile to the same task/run ids by discovery AND to leave
the restarted queue handing the work to a runner. The prior E2E that this
replaces the *claims* of (it answered the first POST and reused one in-memory
RunStore) is kept, honestly reworded, for what it still proves: receipt and
header-key echo across a restart of a completed admission. Executed: core
tasks suite 320 passed / 1 skipped, server api suite 349 passed, the focused
idempotency files green, and the durable file 27 passed *against a real
PostgreSQL* (`MAISTRO_TEST_PG_DSN`), including the wiring-decision test the
previous round left skipped.

Inventory delta vs the recorded round-2 head: core +2, server +1, tests/ +2.

## Repair round 4 (this worktree) — the two round-3 probe findings, closed

The verify pass on `0e4d62af` re-ran the live probes and surfaced two gaps in
the round-3 repairs, both reproduced and closed here (counts recorded in the
delta note `m1-1176-b1-5f9d.md`):

- **Migration 034 adopted a PK-less provisioned table.** A provisioning that
  predates the `PRIMARY KEY` in `ensure_schema` leaves a column-complete
  table with no unique constraint; the reconcile path stamped the revision
  and the store's first `INSERT ... ON CONFLICT (scope_key)` then failed —
  there was nothing to conflict on. The reconcile path now reconstructs the
  primary key (and the downgrade is `IF EXISTS`-idempotent like the rest of
  the chain). New: `test_adopts_runtime_provisioned_table_without_a_primary_key`
  (tests/ +1), plus the hand-executed probe: PK-less table at `032`, upgrade,
  then `ON CONFLICT` insert replay verified against the live server.
- **Production CORS refused the idempotency headers.** The app-level
  `CORSMiddleware` allow-list omitted `Idempotency-Key` and the two
  workspace-scope headers, so a cross-origin preflight carrying a retry's
  identity answered `400 Disallowed CORS headers`. All three are now
  allowed; the wire tests (router-only mounts) could never see this, so the
  new `test_the_idempotency_key_header_is_allowed_cross_origin` inspects the
  production app's middleware config (server +1), and the preflight was
  re-executed against the default allowed origin: `200` with all three
  headers echoed.

The third finding — PR #1214's remote head (`864cb644`) differing from this
branch's local head — is GitHub plumbing this worktree cannot act on (no
push/merge mutations); it is recorded for the campaign driver.

Pre-existing at this branch's head and *not* caused by or fixed in this
repair: `scripts/check-vulture-baseline.py` exits 1 (209 fastapi-route-handler
identities the trusted ledger lacks — the develop merge's route surface
predates the ledger; verified identical at `0e4d62af` and with this salvage,
and present against develop's own ledger). Authorizing that debt is a grant
action outside an implementation repair's scope.

## Repair round 5 (this worktree) — develop merge reconciliation

Develop (`ba2f1f07`, through #1537) carried its own copy of this feature
(#1214, migration `038_task_idempotency`); this branch carried the evolved one
(claim-token fencing, `begin` announcement, ambiguous-window resolution,
Project scoping). The merge reconciliation keeps this branch's implementation
everywhere it was a strict superset (verified symbol-by-symbol: develop's
`idempotency.py` declares no method or constant this side lacks) and ports the
rest:

- **Migration chain.** This branch's `034_task_idempotency` is renumbered onto
  develop's chain as `038` (develop grew `034`–`037` in between); its stronger
  shape (`claim_token`, `completed_at` guard, reconcile-path PK reconstruction)
  is preserved, and the duplicate revision `034` is gone. The chain is linear
  `…→033→034→035→035_outcome_scope_thumb_index→036→037→038` with one head.
- **`TEXT` vs `Text` (real defect, reproduced on live PostgreSQL).** The
  reconcile path's type identity check compared reflected columns against
  `sa.Text`/`sa.BigInteger` — but dialect reflection returns SQLAlchemy's
  uppercase generic classes (`sqltypes.TEXT`, `sqltypes.BIGINT`), so
  `type(actual) is type(expected)` failed for every column of an exactly
  matching table and `alembic upgrade head` died on the very deployments the
  reconcile path exists for. `CLAIM_COLUMNS` now uses the uppercase generic
  spellings (byte-identical DDL, reflection-exact classes); both refusal edges
  still refuse (verified live).
- **`tests/migrations/test_migration_chain.py`.** The four runtime-provisioned
  tables (`capability_approvals`, `capability_bindings`,
  `capability_invocation_effects`, `canonical_workspace_lifecycle`) are
  removed from `EXPECTED_TABLES`: no migration owns them, and the live-catalog
  equality is the chain's product on a clean database — the prior entries only
  held on a dev database where the app had already provisioned its stores (the
  pre-repair finding's "five actual catalog tables"). The stale revision
  assertions (`034`→`038`; the foreign-shape test's impossible `032` after a
  failed upgrade from `033`→`033`) are corrected. Plus the two in-memory
  eviction tests develop had and this branch's rewrite dropped — the bound
  still exists (`_enforce_bound`) and is now tested again (ported verbatim,
  helpers compatible). Dropped as superseded (named, not silently deleted):
  `test_release_returns_a_failed_admissions_claim` (release is token-fenced
  now — covered by the release/takeover tests), `test_complete_and_release_
  carry_the_pending_guard` (the `task_id IS NULL` guard became the token fence
  + `completed_at = 0` guard — covered by the fence tests), and
  `test_the_wire_probes_the_table_before_choosing_postgres` (tier selection
  now follows the spine probe down to SQLite/in-memory — covered by the
  tier-following wiring tests).
- **Source conflicts.** `runs/store.py`, `pg_store.py`, `sqlite_store.py`:
  additive unions (this branch's `find_run_by_task_receipt` + develop's
  occurrence/delegation lookups). `tasks/admission.py`: develop's #1346
  request-id provenance kept alongside this branch's idempotency provenance.
  `tasks/queue.py`: this branch's evolved admission flow; develop's v1
  duplicates would have shadowed it.
- Stale finding closed by the merge evidence: hive-conductor's demo
  `LocalTaskBackend` takes `idempotency_store=self.task_idempotency` from the
  core container (`engine.py:221`), so demo retries reconcile rather than mint
  duplicate Runs.

Measured at this head (vs develop `ba2f1f07`): core `test_idempotency.py` 52
(+16), `test_idempotency_durable.py` 27 (+9), server `test_tasks_idempotency.py`
13 (+6), `tests/migrations` +4 (`TestTheChainSurvivesRuntimeSelfProvisioning`).
Executed: full migration suite 97 passed against a live PostgreSQL 18
(`MAISTRO_TEST_DATABASE_URL` on a clean database), core suite 9942 passed /
638 env-gated skips, server 368 passed, hive-conductor backend 2482 passed,
mypy clean, ruff check/format clean.
