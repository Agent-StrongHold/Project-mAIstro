---
inventory-delta:
  packages/maistro-core/tests: +4
---

# #1177 — canonical evidence in PostgreSQL status listings

Adds the three-backend Run-store conformance regression for tagged/non-finite
Run results: `get_run` and `list_by_status` must expose identical canonical
evidence values.
