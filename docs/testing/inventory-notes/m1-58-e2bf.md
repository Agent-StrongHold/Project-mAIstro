---
inventory-delta:
  packages/maistro-core/tests: +26
---
# m1/58 (#1041): credential pool/rotation moves to Provider selection

Moves the credential pool and key rotation behind Provider selection (#58).
`tests/capabilities/test_credential_routing.py` is new: it proves the
capability effect context resolves slot-specific resolver/executor pairs
through the `CredentialRouting` accessor so credential selection follows the
resolved Binding scope and rotation reacts to real Invocation outcomes.
`tests/credentials/test_router.py` is new: the credential router's own
selection/cooldown/blocked-key behavior, now measured directly since the
router is the seam Providers call. `tests/credentials/test_pool.py` shrinks:
its provider-coupled dispatch cases moved into the two new files, leaving
only pool-mechanics coverage. `tests/credentials/test_protocols.py` loses its
removed-seam cases. Net +26 maistro-core node IDs — the governed credential
path is now covered at the seam that ships, not at the pool that backs it.

<!-- Say what moved and why, not just how much. The count alone hides
     compensating changes; that is the case these notes exist for. -->
