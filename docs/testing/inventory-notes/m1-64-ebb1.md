---
inventory-delta:
  packages/maistro-core/tests: +24
  tests/: +2
---
# m1/64 — episodic memory names its producing Run

`packages/maistro-core/tests +24`: `test_episodic_provenance.py`, new with
this change. Ten tests, parametrized over every `EpisodicStore` backend
(memory/sqlite/postgres) where the criterion applies to all of them and over
the durable two where it asserts on the stored row — 24 collected legs in
total. The two row-reading criteria (NULL-not-`''` absence, the upsert moving
the producer) sit on a `durable_episodic` fixture rather than skipping inside
a 3-way parametrized test: the AC-outcome plugin counts a skipped marked test
as not passing, so an in-test skip would sink the criterion even when every
leg that can apply passed.

`tests/ +2`: `test_episodic_provenance_migration.py`, the PostgreSQL half of
the same change — migration 031 up/down round trip against a real server,
skipping without `MAISTRO_TEST_DATABASE_URL` like its siblings. The spec
records why these are deliberately not AC-marked: a migration is a history
file, not a module the reachability graph knows.
