---
inventory-delta:
  packages/hive-conductor/backend/tests: +16
  packages/maistro-core/tests: -1
  tests/: +6
---
# m1-840-roster-1219 — diff-coverage salvage for the roster PR, plus a ledger correction

`packages/hive-conductor/backend/tests` +16 and `tests/` +6 are the
diff-coverage work for this branch's Coverage-gate failure: fail-closed
refusal arms pinned at the HTTP seam (4 tests over `routes.agents`
`_store_or_refuse`), at the chat-tool seam (8 tests over save-as /
create-button / modify-button refusals and the turn's `_request_workspace_id`
scope), the `EngineService.agent_port` read-side seam (2), and both arms of
the bridge's relative-vs-absolute `maistro_agents_dir` resolution (2). For
`tests/` +6: the `check-agent-store-writes.py` gate's `main()` entry point,
violation rendering, `_relative`'s off-root fallback, the syntax-error
degradation, the non-assignment subscript Store context, and the `__main__`
console path.

`packages/maistro-core/tests` -1 is NOT a test this branch removed — this
branch's diff adds only test files under `hive-conductor` and `tests/`. It
corrects a pre-existing ledger over-count that this branch's CI run was the
first to surface: at 424c0749 the `test` job's inventory step failed with
exactly `DRIFT packages/maistro-core/tests: expected 9549, collected 9548`,
while every other suite matched. The over-count traces to #1208
(`m2-1172-a7.md`, maistro-core +16): that merge's diff adds 16 and removes 1
test function across `test_pg_strikes.py`, `test_strike_recovery.py`,
`test_strike_tracker_conformance.py`, and `test_container_security_wiring.py`
— a net of +15, none parametrized — but recorded +16. On `develop` the drift
was masked because the `test` job was already failing one step earlier (the
`tests/` run), so the inventory step never executed there.
