---
inventory-delta:
  packages/maistro-core/tests: +31
  formal/: +1
---

# m2-1165-a1 — Sentinel permission table fail-closed default (#1165)

**+13 `packages/maistro-core/tests`** — pins for the fail-closed table-miss
semantics (ADR-072726-0d6b implemented), spread so that flipping unknown-tool
behavior back to allow fails at four independent layers:

- `tests/security/test_sentinel_policy.py` (+9): `can_use_tool` /
  `check_permission` deny on table miss with the explicit `allow_on_miss=True`
  compatibility mode pinned (including that it still honours explicit
  entries, hard-denies empty role sets, and warns when armed);
  `pre_call` denies unknown tools on an empty table AND on a governed
  `build_permission_table` production table while explicit grants still
  allow; `authorize` denies/reallows symmetrically.
- `tests/security/test_permission_policy.py` (+1): the formal-I6 core mirror
  flips — `test_absent_entry_denies_by_default` — plus
  `test_governed_table_still_grants_explicit_entries` so fail-closed is a
  default, not a blanket deny.
- `tests/test_container_security_wiring.py` (+2): production composition pins —
  `test_wired_sentinel_with_default_config_denies_everything` (the
  previously-allow-all case: an empty governed table authorizes nothing) and
  `test_security_config_cannot_arm_allow_on_miss` (no configuration field can
  express the compatibility mode). The old
  `test_wired_sentinel_permits_unlisted_tool` became
  `test_wired_sentinel_denies_unlisted_tool` (rename, not a count change).- `tests/graph/nodes/test_agent_synth_dag.py` (+1):
  `test_bare_node_fails_closed_without_governed_permission` — the bare
  production construction refuses to approve/dispatch a synthesized DAG with
  no governed permission source, and succeeds once an explicit decision
  grants it.

**+1 `formal/`** — invariant I6 amendment (its own commit, `3d593b17`, per the
ADR's sequencing rule): `test_no_permission_entry_compat_mode_allows` pins the
explicit compatibility mode in the formal model; the three allow-on-miss
properties were amended in place (renames, no count change):
`test_no_permission_entry_means_denied`,
`test_can_use_tool_no_entry` (now asserts deny),
`test_pre_call_empty_table_denies_by_default`.

**Repair follow-up (+18 more, same branch): reconciliation with canonical
capability state and runtime revoke (criteria 4-5/7 of #1165).**

- `tests/security/test_permission_source.py` (+9, new file): the
  decision-time `PermissionSource` seam — `CapabilityPermissionSource`
  projects the canonical `CapabilityRegistry`'s live enable state over the
  deployment table (disabling a slot removes the tool at the next resolution,
  re-enabling restores exactly the base decision, the adapter can only remove
  authority and derives its gating from the registry itself, no
  hand-maintained mapping), and `resolve_live_table` collapses every
  failure mode (raise, `None`) to a fail-closed no-decision.
- `tests/security/test_sentinel_policy.py` (+5): Sentinel honors a wired
  source (explicit allow stands, miss denies), post-initialization capability
  revoke flips the very next `pre_call` AND `authorize` decision with no
  restart, and an unavailable source denies even though the static table has
  an entry — a stale snapshot must not outvote a revoke.
- `tests/test_container_security_wiring.py` (+4): on the real production
  composition — the container's Sentinel holds a live source backed by the
  SAME `CapabilityRegistry` the container exposes;
  `capabilities.set_enabled("approval", False)` revokes an explicitly granted
  tool at the next decision and re-enabling restores it (no restart); the
  live source never grants tools the table does not name; and the test
  harness (the second composition path) wires the identical live source.

Existing suites that exercise non-permission Sentinel behavior (tier ladder,
delegability, dag shape, rlphd, elevation grants, approver graph, argument
limits, synth_dag mechanics, durable depth walks) construct their Sentinels
with the explicit `allow_on_miss=True` compatibility mode, each marked
`COMPATIBILITY (#1165)` — non-production constructions, explicitly marked per
the issue's acceptance.

Verified: +13 and +1 match `pytest --collect-only` drift against the recorded
baseline (9551→9564, 421→422); every other suite unchanged.

**CI repair (same branch, no test-count change): radon ratchet regression on
`Sentinel.authorize`.** The criteria-4/5 repair pushed `authorize` to
cyclomatic complexity 12 against a trusted baseline of 11
(`scripts/check-radon-baseline.py` — the exact step that failed in CI's
quality-gate job). Fixed by a behavior-preserving extraction, not a ledger
raise: the permission gate moved into `Sentinel._permission_denial_reason`
(same two deny reasons, same decision order — live source first, unavailable
source denies, capability miss denies), returning `authorize` to its
baselined complexity 11 and keeping the trusted floor unchanged. No
`radon-baseline.json` or `ratchet-authorizations.json` entry changed. The
mutation check was re-proven after the refactor: reverting the table-miss
branch in `AuthContext.can_use_tool` to allow fails 17 tests across four
layers (core units, formal I6, container wiring, synth_dag).
