---
inventory-delta:
  packages/maistro-core/tests: +10
---
# m2-1172-a7 — PostgreSQL strike recovery and canonical database binding

The PostgreSQL strike tests now cover administrative unlock/enable/removal,
unknown-user behavior, and the canonical `DATABASE_URL` selection while
explicitly ignoring `DEPLOY_TARGET_DB_URL`. Four recovery-service tests cover
self-service and non-admin denials, scoped authorized recovery, audit evidence,
and the complete enable/remove lifecycle. Container wiring adds one integration
check for the recovery service. The conformance durability test also
verifies a lockout can be cleared and remains cleared after a fresh tracker.
