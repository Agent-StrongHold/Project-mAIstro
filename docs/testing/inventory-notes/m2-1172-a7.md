---
inventory-delta:
  packages/maistro-core/tests: +16
  packages/maistro-server/tests: +2
---
# m2-1172-a7 — PostgreSQL strike recovery and canonical database binding

The PostgreSQL strike tests now cover administrative unlock/enable/removal,
unknown-user behavior, appeal persistence/refusal, and the canonical
`DATABASE_URL` selection while explicitly ignoring `DEPLOY_TARGET_DB_URL`. The
recovery-service tests cover self-service and non-admin denials, scoped
authorized recovery, re-locking after unlock, concurrent violations and unlock,
a combined PostgreSQL lifecycle, audit evidence, and the complete
in-memory enable/remove lifecycle. Readiness tests identify disabled and
in-memory strike tracker backends without mutating them. Container wiring adds
one integration check for the recovery service. The conformance durability test
also verifies a lockout can be cleared and remains cleared after a fresh tracker.
