---
inventory-delta:
  packages/hive-conductor/backend/tests: +10
  packages/maistro-core/tests: +3
---
# m2-1185-a — canonical password policy at first-run setup

The Conductor backend suite's recorded delta is now ten node IDs in
`test_setup_guard.py`: the five setup-guard IDs already recorded for the
implementation are joined by five focused route and model tests covering the
hardware-preset validator, direct
setup password validation, vault persistence, and both preset adapters. The
maistro-core security suite gains three node IDs covering the seven-character
rejection and eight-character-or-longer acceptance boundary of the shared
`validate_password` policy. The implementation also moves the existing
ordinary registration length check into that shared validator, so setup and
registration cannot drift.
