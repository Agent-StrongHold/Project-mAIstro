---
inventory-delta:
  packages/hive-conductor/backend/tests: +3
  packages/maistro-core/tests: +13
---
# m2-329-p0p1

Crypto-bound approvals, phases 0+1 (#329): the accepted Hybrid ADR
(ADR-090726-9a4e) plus four seam fixes that make the existing human-decision
seams fail closed and name their principals.

`packages/maistro-core/tests +13`: `test_human_verdict_fail_closed.py`, new —
eleven cases over the three verdict nodes (`human.approve_draft`,
`human.delegate_to_role`, `human.review_and_edit`): a missing, blank, or
non-string verdict re-pauses rather than approving, an explicit verdict still
completes (including delegate routing and review edits), a verdict-less
delegate answer never consults the role resolver, and an unrecognized verdict
string fails the node rather than approving. Two cases in
`test_durable_approval.py`: `resolve()` without an `actor` raises TypeError on
both backends and leaves the record PENDING — the actor argument no longer
defaults, so the test pins the seam rather than any one implementation.

`packages/hive-conductor/backend/tests +3`: `test_hitl_door.py` — the answer
and cancel audit entries name the authenticated session principal
(`testadmin`) instead of the literal `"system"`, and a request with no
resolvable principal records `"unauthenticated"` rather than overclaiming a
system actor.
