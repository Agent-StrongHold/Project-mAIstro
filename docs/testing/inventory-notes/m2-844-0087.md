---
inventory-delta:
  packages/maistro-core/tests: +63
---
# m2-844-0087

All +63 are outcome-scope tests for #844 (tenant/project scope in SQLite
Outcome reads with PostgreSQL parity); nothing was removed, so there are no
compensating changes to read past.

- `tests/persistence/test_outcome_scope_conformance.py` (new, +36): the
  two-tenant/two-project conformance suite, parametrized over both durable
  backends — 18 test bodies x {sqlite, postgres}. The PostgreSQL leg skips
  without `MAISTRO_TEST_PG_DSN`, same as the other persistence suites.
- `tests/persistence/test_sqlite_outcomes.py` (+18): 11 scope/fail-closed/
  query-plan assertions plus a 7-way parametrized "ambiguous scope raises"
  case covering every read that takes a scope argument.
- `tests/persistence/test_pg_outcomes.py` (+9): the SQL-composition and
  days-zero-WHERE assertions for the shared scope rule, plus the same 7-way
  ambiguous-scope parametrization mirrored on the fake-connection store.
