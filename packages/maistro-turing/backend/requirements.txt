# Install the runtime library and its maistro-core dependency with the app.
# The backend is launched from this directory, so its explicit application
# package is importable without a path-order workaround.
-e ..
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.28
pydantic>=2.0
