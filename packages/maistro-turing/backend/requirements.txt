# Install the runtime library and its maistro-core dependency with the app.
# Install both the reusable runtime and the explicit application package.
# The second editable install makes the documented uvicorn command work from a
# clean environment without relying on the current directory as an import root.
-e ..
-e .
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.28
pydantic>=2.0
