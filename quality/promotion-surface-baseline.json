{
  "_comment": "Modules reachable from the promotion/execution path that deliberately do not escalate to adversarial review. Every entry needs a reason, and the reason must be about what the module DECIDES or INSTALLS, not only where a policy is written down: settings.py and http.py were tolerated here on reasons that were true and beside the point (Codex, #513). See scripts/check-promotion-surface.py.",
  "tolerated": {
    "packages/maistro-core/src/maistro/quota/billing.py": "Token-cost accounting. It bounds what a run may spend; it cannot change whether a candidate is promoted or what score it receives.",
    "packages/maistro-core/src/maistro/quota/rate_profile.py": "Per-provider rate limits. Same reasoning as billing.py: a spend and pacing control, not an acceptance control.",
    "packages/maistro-core/src/maistro/quota/tracker.py": "Records token usage per provider per billing cycle. Reachable because every model call is metered; no promotion decision reads it.",
    "packages/maistro-core/src/maistro/tools/result.py": "The shared ToolResult dataclass. A data carrier with no policy in it.",
    "packages/maistro-core/src/maistro/types/errors.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/agent.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/config.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/feedback.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/intent.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/memory.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/model.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/prompt.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/reactor.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/security.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/session.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/skill.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/spec.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/types/tool.py": "A shared dataclass module in maistro/types/. Data carriers with no policy in them, reached transitively by everything that passes a typed value; the same reasoning errors.py already carried. Newly visible because the import walker stopped discarding relative imports and package initializers (#513), not because anything moved onto the promotion path.",
    "packages/maistro-core/src/maistro/observability/logging.py": "Telemetry: logging, metrics, trace proxying, replay and PII tiers. Reached because the loop is instrumented, not because it decides anything -- verified by grep for promotion/threshold/approve/verdict logic across the package, which finds none. Newly visible for the same walker repair (#513).",
    "packages/maistro-core/src/maistro/observability/metrics.py": "Telemetry: logging, metrics, trace proxying, replay and PII tiers. Reached because the loop is instrumented, not because it decides anything -- verified by grep for promotion/threshold/approve/verdict logic across the package, which finds none. Newly visible for the same walker repair (#513).",
    "packages/maistro-core/src/maistro/observability/proxy.py": "Telemetry: logging, metrics, trace proxying, replay and PII tiers. Reached because the loop is instrumented, not because it decides anything -- verified by grep for promotion/threshold/approve/verdict logic across the package, which finds none. Newly visible for the same walker repair (#513).",
    "packages/maistro-core/src/maistro/observability/replay.py": "Telemetry: logging, metrics, trace proxying, replay and PII tiers. Reached because the loop is instrumented, not because it decides anything -- verified by grep for promotion/threshold/approve/verdict logic across the package, which finds none. Newly visible for the same walker repair (#513).",
    "packages/maistro-core/src/maistro/observability/tiers.py": "Telemetry: logging, metrics, trace proxying, replay and PII tiers. Reached because the loop is instrumented, not because it decides anything -- verified by grep for promotion/threshold/approve/verdict logic across the package, which finds none. Newly visible for the same walker repair (#513)."
  }
}
