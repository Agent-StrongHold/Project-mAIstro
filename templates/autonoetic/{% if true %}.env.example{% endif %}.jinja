# Copy to .env (which is gitignored) and populate through your secret manager.
# Empty values are deliberate placeholders, not usable credentials.
TURING_SELF_ID=
LITELLM_BASE_URL=
LITELLM_VIRTUAL_KEY=
{% if dossier_store == 'obsidian' -%}
OBSIDIAN_VAULT_PATH=
{% endif -%}
